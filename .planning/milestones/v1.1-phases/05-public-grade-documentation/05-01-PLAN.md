---
plan_id: 05-01
phase: 05-public-grade-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/examples/daily-log.md
  - docs/examples/weekly-report.md
  - docs/examples/monthly-report.md
  - docs/examples/self-review.md
autonomous: true
requirements: [DOCS-05]
must_haves:
  truths:
    - "A reader can open docs/examples/daily-log.md and see a faithful self-wiki daily log shape with EXAMPLE-NNN tickets, session blocks, Note [HH:MM] lines, and (for at least one block) a complete close-out (Ended/Duration/Completed)."
    - "A reader can open docs/examples/weekly-report.md and see a faithful weekly-report shape: H1, one-paragraph Sources line, Theme of the week with a Ticket→Layer→Outcome table, Notable architectural decisions, Process / tooling improvements, Lessons learned, Risks / carry-over, Quick metrics."
    - "A reader can open docs/examples/monthly-report.md and see a faithful monthly-report shape: H1, Sources line, Theme(s) of the month (numbered when multiple), Notable architectural decisions, Process / tooling improvements, Lessons learned, Risks / carry-over, Quick metrics, and a final ## Sources block grouped by type."
    - "A reader can open docs/examples/self-review.md and see a faithful self-review shape: H1 with cycle name + date range, Sources line, the three Liferay review questions as ## H2 headings, every Section 1 accomplishment ending in a value-tag clause naming one or more of the five Liferay values, and a final ## Sources block grouped by type."
    - "The four files form one coherent fictional storyline — the same fictional engineer, the same set of EXAMPLE-NNN tickets (at least EXAMPLE-001, EXAMPLE-002, EXAMPLE-003), where the daily log's activity surfaces in the weekly's themes, the weekly's themes surface in the monthly, and the monthly is one of the sources the self-review cites."
    - "Every file opens with an HTML-comment disclaimer that survives raw view but is invisible when rendered, explaining that the file is a fictional scrubbed example and pointing at the README for storyline context."
    - "No real Liferay ticket prefix (LPD-, LPP-, LPS-, LRELEASE-) appears anywhere in any of the four files; every ticket is an EXAMPLE-NNN ticket."
    - "No real person's name, no real PR title, no real internal Liferay system name appears in any file. No `Liferay, Inc.` string anywhere."
  artifacts:
    - path: "docs/examples/daily-log.md"
      provides: "Fictional daily log demonstrating session lifecycle, notes, ticket detection"
      contains: "EXAMPLE-001"
    - path: "docs/examples/weekly-report.md"
      provides: "Fictional weekly synthesis output (shape mirrors src/templates/prompts/weekly-report.md)"
      contains: "## Theme of the week"
    - path: "docs/examples/monthly-report.md"
      provides: "Fictional monthly synthesis output (shape mirrors src/templates/prompts/monthly-report.md)"
      contains: "## Theme(s) of the month"
    - path: "docs/examples/self-review.md"
      provides: "Fictional self-review draft (shape mirrors src/templates/prompts/self-review.md)"
      contains: "Produce Excellence"
  key_links:
    - from: "docs/examples/daily-log.md"
      to: "docs/examples/weekly-report.md"
      via: "EXAMPLE-NNN tickets and themes recur (storyline continuity)"
      pattern: "EXAMPLE-001"
    - from: "docs/examples/weekly-report.md"
      to: "docs/examples/monthly-report.md"
      via: "monthly's themes/recurring tickets cite weekly content"
      pattern: "EXAMPLE-00"
    - from: "docs/examples/monthly-report.md"
      to: "docs/examples/self-review.md"
      via: "self-review Sources lists the monthly file"
      pattern: "Reports/YYYY-MM.md"
---

<objective>
Land the four `docs/examples/*.md` reference artifacts that satisfy DOCS-05 and form the canonical scrubbed examples that the Wave-2 README rewrite will reference. All four files are synthesized from scratch (no real-vault leakage) and form one coherent fictional storyline so a reader walking from daily to self-review sees the synthesis chain working.

Purpose: A Liferay engineer evaluating self-wiki opens these four files before deciding to install. They must (a) prove the tool produces useful output, (b) demonstrate the four artifact shapes the real prompts produce, and (c) carry zero leakage risk — every ticket, name, and title is invented.

Output: `docs/examples/daily-log.md`, `docs/examples/weekly-report.md`, `docs/examples/monthly-report.md`, `docs/examples/self-review.md`. Each opens with an HTML-comment disclaimer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-public-grade-documentation/05-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@src/templates/prompts/weekly-report.md
@src/templates/prompts/monthly-report.md
@src/templates/prompts/self-review.md
@src/core/logger.js
@src/core/topics.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/examples/ directory and synthesize daily-log.md + weekly-report.md (storyline anchor)</name>
  <files>docs/examples/daily-log.md, docs/examples/weekly-report.md</files>
  <read_first>
    - .planning/phases/05-public-grade-documentation/05-CONTEXT.md (full file — every D-EXAMPLES-* lock and `<specifics>` item)
    - src/templates/prompts/weekly-report.md (the real weekly synthesis prompt — the fictional weekly MUST mirror this prompt's output shape)
    - src/core/logger.js (the source of truth for the daily-log session-block structure: `## Session N — Task: <label>`, `- Started: HH:MM`, `- Note [HH:MM]: ...`, `- Ended: HH:MM`, `- Duration: N min`, `- Completed: ✅` or `- Interrupted: ⚠️`, the `<!-- session-N-open -->` sentinel for an open block)
    - src/core/topics.js (how notes get folded into Tickets/EXAMPLE-NNN.md — the topic-page section marker `## YYYY-MM-DD — Session N`)
    - .planning/codebase/CONVENTIONS.md §"Sentinel-Comment Pattern" and §"Naming Conventions" (markdown sentinels / markers section — what lines a real daily log contains)
  </read_first>
  <action>
    Create the `docs/examples/` directory (it does not exist yet — this is the first `docs/` subtree in the repo) and synthesize two fictional reference artifacts that form the storyline anchor.

    **`docs/examples/daily-log.md`** — represents a single day's `Daily/YYYY-MM-DD.md` file in a fictional engineer's vault.

    Header:
    1. First line: an HTML comment disclaimer that is invisible in rendered markdown but visible in raw view. Use this exact template (adjust the second line if the storyline calls for it but keep the disclaimer + EXAMPLE-NNN + README pointer):
       ```
       <!--
       This is a fictional scrubbed example for self-wiki.
       `EXAMPLE-NNN` tickets are made up. For the full storyline see ../../README.md.
       -->
       ```
    2. Second header line (per D-EXAMPLES-REGEX-DEMO): an HTML comment declaring the vault config used so a reader understands ticket detection on `EXAMPLE-NNN` is faithful:
       `<!-- vault config: { ticketRegex: 'EXAMPLE-\\d+' } -->`
    3. Title line: `# Daily/YYYY-MM-DD.md` (literal placeholder `YYYY-MM-DD` per D-EXAMPLES-DATES — do NOT use 2026-04-21 or any other concrete date).

    Body — at least two sessions, ideally three, on the same fictional day. Use the real session-block format from `src/core/logger.js`:
    - Session 1: closed and completed. Header `## Session 1 — Task: EXAMPLE-001 — <fictional task summary>`, then `- Started: 09:00`, two to four `- Note [HH:MM]: <text>` lines that read like real decision/outcome notes (e.g. "looked at the bug", "decided to extract a helper", "PR #N opened against …"). Close with `- Ended: 12:08`, `- Duration: 188 min`, `- Completed: ✅`. **NO sentinel comment** on this block (it is closed).
    - Session 2: also closed but `Interrupted: ⚠️` (terminal killed without graceful exit, then `close-orphans` finalized it — be faithful to that path). Header references `EXAMPLE-002`. At least one note. Include `- Last activity: HH:MM` strip is implicit (logger strips it on close); just emit `- Ended:`, `- Duration:`, `- Interrupted: ⚠️`.
    - Session 3 (optional but improves the storyline): currently open. Header references `EXAMPLE-003` (or revisits `EXAMPLE-001` via a `Switched:` line). Has `- Started: HH:MM`, at least one `- Note [HH:MM]:` line, AND ends with the literal `<!-- session-3-open -->` sentinel so the reader sees an open block in the wild.

    Concrete HH:MM times and weekday-realistic ordering are encouraged (per `<specifics>`: the *date anchor* is the placeholder; the *internal rhythm* is concrete). Notes should reference at least one PR number (e.g. `PR #421`) and exhibit a "force-pushed" mention so the weekly report has something to count.

    **`docs/examples/weekly-report.md`** — represents `Reports/YYYY-Www.md`, the synthesized weekly. Must mirror the OUTPUT structure of `src/templates/prompts/weekly-report.md` section-for-section. Header:
    1. The same fictional-example HTML-comment disclaimer (single-block, three-line form) at the top.
    2. Title line: `# Weekly Report — Mon YYYY-MM-DD → Fri YYYY-MM-DD` — keep the literal `YYYY-MM-DD` placeholder on both ends; the prompt asks for a Mon→Fri range and the example must respect that.

    Body sections in order, every section non-empty (so the reader sees the full shape):
    - `Sources:` one-paragraph line listing the daily-log filenames the report drew from. Use placeholder filenames `Daily/YYYY-MM-DD.md` (the same placeholder; you may distinguish "Mon", "Tue", … in prose) — do NOT invent concrete dates. Note one missing day to demonstrate the prompt's "Note any missing days" behaviour.
    - `## Theme of the week` — H2. One small markdown table (Ticket | Layer | Outcome) with rows for EXAMPLE-001, EXAMPLE-002, EXAMPLE-003. Followed by 1-2 short paragraphs of prose. Themes must be traceable to notes in the synthesized daily-log.md so the storyline chains.
    - `## Notable architectural decisions` — bulleted list. Each bullet leads with the ticket/decision in **bold**, then 2-4 lines of what was decided + why + the rejected alternative. At least two decisions, each tied to an EXAMPLE-NNN ticket.
    - `## Process / tooling improvements` — bullets of new capability acquired. At least one item.
    - `## Lessons learned` — bullets, each lesson in **bold** then 1-2 lines of context. At least one lesson.
    - `## Risks / carry-over` — at least one concrete item, citing a specific session number and date (placeholder day-of-week is fine: "Wed session 2").
    - `## Quick metrics` — deterministic-shape metrics block. Use the same shape `src/commands/report.js` produces: a few lines like "Sessions: N", "Tickets: EXAMPLE-001, EXAMPLE-002, EXAMPLE-003", "PR refs: #421, #422", "Force-pushes: N". Numbers are invented but must be self-consistent with the daily-log body (force-push count matches the force-push notes; PR refs match what notes mention).

    Storyline lock (D-EXAMPLES-CONTINUITY): EXAMPLE-001, EXAMPLE-002, EXAMPLE-003 MUST appear in both files. The weekly's Theme of the week table cites them as rows. The weekly's Notable architectural decisions cite at least one of them. The daily-log's notes are the evidence for the weekly's bullets — pick decisions and lessons in the weekly that you can point back at a specific note line in the daily-log. The same fictional engineer is the implicit narrator in both.

    Liferay-specific constraints (inherited from Phase 04 D-LEG-01-OVERRIDE and the no-real-names rule):
    - No `Liferay, Inc.` string. No real Liferay engineer names. No real internal product names (no "DXP", no "Faro", no "Liferay Cloud"). The fictional context should read like generic SaaS/dev tooling.
    - No real ticket prefixes — no `LPD-`, no `LPP-`, no `LPS-`, no `LRELEASE-`. Only `EXAMPLE-NNN`.
    - No emojis except the `✅` and `⚠️` glyphs that the logger itself emits on close lines (those are part of the file format, not decoration).
  </action>
  <verify>
    <automated>test -f docs/examples/daily-log.md && test -f docs/examples/weekly-report.md && grep -q 'EXAMPLE-001' docs/examples/daily-log.md && grep -q 'EXAMPLE-001' docs/examples/weekly-report.md && grep -q '## Theme of the week' docs/examples/weekly-report.md && grep -q '## Quick metrics' docs/examples/weekly-report.md && grep -q 'fictional scrubbed example' docs/examples/daily-log.md && grep -q 'fictional scrubbed example' docs/examples/weekly-report.md && ! grep -qE '\b(LPD|LPP|LPS|LRELEASE)-[0-9]+\b' docs/examples/daily-log.md && ! grep -qE '\b(LPD|LPP|LPS|LRELEASE)-[0-9]+\b' docs/examples/weekly-report.md && ! grep -qi 'liferay, inc' docs/examples/daily-log.md && ! grep -qi 'liferay, inc' docs/examples/weekly-report.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/examples/daily-log.md` succeeds.
    - `test -f docs/examples/weekly-report.md` succeeds.
    - `head -1 docs/examples/daily-log.md` is the opening `<!--` of the disclaimer comment.
    - `grep -c 'EXAMPLE-001' docs/examples/daily-log.md` is at least 1.
    - `grep -c 'EXAMPLE-002' docs/examples/daily-log.md` is at least 1.
    - `grep -c 'EXAMPLE-003' docs/examples/daily-log.md` is at least 1.
    - `grep -q '<!-- vault config: { ticketRegex: ' docs/examples/daily-log.md` succeeds (D-EXAMPLES-REGEX-DEMO).
    - `grep -q '# Daily/YYYY-MM-DD.md' docs/examples/daily-log.md` succeeds (literal placeholder per D-EXAMPLES-DATES).
    - `grep -q '<!-- session-' docs/examples/daily-log.md` succeeds (at least one open sentinel demonstrates the open-block shape).
    - `grep -qE '^- Note \[[0-9]{2}:[0-9]{2}\]:' docs/examples/daily-log.md` succeeds (Note line shape matches `src/core/logger.js`).
    - `grep -q '^- Completed: ' docs/examples/daily-log.md` and `grep -q '^- Interrupted: ' docs/examples/daily-log.md` both succeed (at least one of each close status).
    - `grep -q '^# Weekly Report — ' docs/examples/weekly-report.md` succeeds.
    - `grep -q '^## Theme of the week' docs/examples/weekly-report.md` succeeds.
    - `grep -q '^## Notable architectural decisions' docs/examples/weekly-report.md` succeeds.
    - `grep -q '^## Process / tooling improvements' docs/examples/weekly-report.md` succeeds.
    - `grep -q '^## Lessons learned' docs/examples/weekly-report.md` succeeds.
    - `grep -q '^## Risks / carry-over' docs/examples/weekly-report.md` succeeds.
    - `grep -q '^## Quick metrics' docs/examples/weekly-report.md` succeeds.
    - `grep -E '\b(LPD|LPP|LPS|LRELEASE)-[0-9]+\b' docs/examples/daily-log.md docs/examples/weekly-report.md` returns no matches.
    - `grep -i 'liferay, inc' docs/examples/daily-log.md docs/examples/weekly-report.md` returns no matches.
    - `grep -q 'fictional scrubbed example' docs/examples/daily-log.md` and `grep -q 'fictional scrubbed example' docs/examples/weekly-report.md` both succeed (per-file disclaimer per D-EXAMPLES-SELF-DESCRIBE).
  </acceptance_criteria>
  <done>Two files exist under `docs/examples/`. Both pass the grep acceptance criteria. The weekly report's bullets are traceable to notes in the daily log (same EXAMPLE-NNN tickets, same PR refs).</done>
</task>

<task type="auto">
  <name>Task 2: Synthesize monthly-report.md and self-review.md (storyline continuation + Liferay values)</name>
  <files>docs/examples/monthly-report.md, docs/examples/self-review.md</files>
  <read_first>
    - .planning/phases/05-public-grade-documentation/05-CONTEXT.md (D-EXAMPLES-* locks, especially D-EXAMPLES-CONTINUITY and the Claude's Discretion bullet on the fictional engineer's voice in the self-review)
    - src/templates/prompts/monthly-report.md (the real monthly synthesis prompt — output shape including the ## Sources section grouped by type)
    - src/templates/prompts/self-review.md (the real self-review prompt — three question H2s, value-tag clause format `- **<accomplishment>** — <Value>[, <Value>]`, `*(source: <file>[, <file>])*` italics, ## Sources block grouped by type)
    - docs/examples/daily-log.md AND docs/examples/weekly-report.md (created by Task 1 — these MUST be the storyline anchors that the monthly and self-review chain from)
  </read_first>
  <action>
    Synthesize the two remaining `docs/examples/*.md` reference artifacts. Both continue the EXAMPLE-001/002/003 storyline established in Task 1 — same fictional engineer, same tickets recurring across weeks, same themes deepening across months.

    **`docs/examples/monthly-report.md`** — represents `Reports/YYYY-MM.md`. Must mirror the output structure of `src/templates/prompts/monthly-report.md` section-for-section.

    Header:
    1. The standard three-line HTML-comment fictional-example disclaimer.
    2. Title: `# Monthly Report — <Fictional Month> YYYY` — use a fictional month-name-of-the-year placeholder. Per D-EXAMPLES-DATES the *date anchor* is a placeholder; you may use a real month name (e.g. "April YYYY") because the actual locked filename is `Reports/YYYY-MM.md`. Inside the body, use weekly references like `2026-W14` ONLY if absolutely necessary; prefer placeholder weeks like `YYYY-W14`. Keep filenames literal-placeholder; in-narrative weekday/week numbers may be concrete-shaped for readability.

    Body sections in order, every section non-empty:
    - `Sources:` one-paragraph line listing fictional weekly-report filenames (`Reports/YYYY-W14.md`, `Reports/YYYY-W15.md`, `Reports/YYYY-W16.md`, `Reports/YYYY-W17.md` — the four ISO weeks overlapping a month) and the topic-page filenames it drew from (`Tickets/EXAMPLE-001.md`, `Tickets/EXAMPLE-002.md`, `Tickets/EXAMPLE-003.md` — at minimum). Note one missing week (faithful to the prompt's "Some weeks may be flagged as missing").
    - `## Theme(s) of the month` — two themes is the sweet spot; use the `### Theme 1: <name>` / `### Theme 2: <name>` H3 sub-structure the prompt asks for when multiple themes. Each theme has a Ticket/Topic → Layer → Outcome table, then 1-2 paragraphs of prose. EXAMPLE-001 should recur as a "thread" the monthly tracks across weeks (so it shows up as a multi-week ticket — the storyline arc).
    - `## Notable architectural decisions` — bullets, each in **bold** ticket/decision name, 2-4 lines. At least two decisions, each citing an originating week (e.g. `(2026-W15)` or, if you've chosen full placeholders, `(YYYY-W15)` — pick one and stay consistent across the file).
    - `## Process / tooling improvements` — at least one bullet.
    - `## Lessons learned` — at least one lesson in **bold** + 1-2 lines.
    - `## Risks / carry-over` — at least one item, citing week and ticket.
    - `## Quick metrics` — deterministic-shape metrics block matching `src/core/metrics.js` shape: Sessions, Tickets, PR refs, Force-pushes, Days-with-logs, Components touched. Self-consistent with the weekly's totals when scaled up to a month.
    - `## Sources` — final aggregated source list, grouped by type, per the prompt's literal sub-block format:
      ```
      ### Weekly reports
      - `Reports/YYYY-W14.md`
      - ...
      ### Topic pages
      - `Tickets/EXAMPLE-001.md`
      - ...
      ```

    **`docs/examples/self-review.md`** — represents `Reviews/YYYY-cycle1.md`. Must mirror the output structure of `src/templates/prompts/self-review.md`.

    Header:
    1. The standard three-line HTML-comment fictional-example disclaimer.
    2. Title: `# Self-Review — YYYY-cycle1 (YYYY-01-01 → YYYY-04-30)` — keep the literal `YYYY` placeholder per D-EXAMPLES-DATES. The cycle name is `YYYY-cycle1`; the date range mirrors a real cycle-1 window but with placeholder year.

    Body sections in order:
    - `Sources:` one-paragraph line listing the monthly filenames (`Reports/YYYY-01.md`, `Reports/YYYY-02.md`, `Reports/YYYY-03.md`, `Reports/YYYY-04.md`), the weekly filenames (a subset, at least four), and the topic pages (`Tickets/EXAMPLE-001.md`, `Tickets/EXAMPLE-002.md`, `Tickets/EXAMPLE-003.md`). At minimum, the SAME monthly file referenced as a source here MUST be cited in the `## Sources` block at the bottom.
    - `## 1. What have you accomplished since your last review? What work are you proud of?` — H2. At least three bullet accomplishments. EVERY bullet MUST end with the value-tag clause in the exact format `- **<accomplishment>** — <Value>[, <Value>]` using the exact five Liferay value names from `src/templates/prompts/self-review.md` (`Produce Excellence`, `Lead by Serving`, `Value People`, `Grow & Get Better`, `Stay Nerdy`). Every bullet also carries the `*(source: <file>[, <file>])*` italic clause BEFORE the value-tag clause, citing the monthly/weekly/topic-page files. At least one bullet should be multi-value-tagged. All five Liferay values MUST appear at least once across the section.
    - `## 2. Since your last review, what is something you would have done differently in your work?` — H2. At least two bullets, each leading with the lesson in **bold**, 1-2 lines of context, and a `*(source: …)*` italic clause.
    - `## 3. What is your current area of focus as you "Grow & Get Better", and how will that positively impact your work?` — H2. A short prose paragraph (3-6 sentences) synthesizing a focus area from recurring patterns (e.g. "three monthlies mention testing gaps → focus on TDD"), followed by 2-4 sub-bullets each with a `*(source: …)*` italic clause.
    - `## Sources` — final aggregated source list, grouped by type, using the literal sub-block format from the prompt:
      ```
      ### Monthly reports
      - `Reports/YYYY-01.md`
      - `Reports/YYYY-02.md`
      - `Reports/YYYY-03.md`
      - `Reports/YYYY-04.md`

      ### Weekly reports
      - `Reports/YYYY-W14.md`
      - ...

      ### Topic pages
      - `Tickets/EXAMPLE-001.md`
      - `Tickets/EXAMPLE-002.md`
      - `Tickets/EXAMPLE-003.md`
      ```

    Voice + storyline locks (D-EXAMPLES-CONTINUITY + Claude's Discretion):
    - First-person, present-perfect ("I refactored…", "I introduced…", "I helped a teammate ship…"), paste-ready for a Liferay HR form.
    - No marketing-speak, no buzzwords. Match the terse senior-engineer tone the real `self-review` prompt produces.
    - EXAMPLE-001, EXAMPLE-002, EXAMPLE-003 MUST appear in the self-review's accomplishments and Sources block. Accomplishments must read as plausible continuations of the threads established in the daily/weekly/monthly files (e.g. the EXAMPLE-001 ticket that recurred across multiple weeks in the monthly should land as a multi-month accomplishment here).

    No-leakage locks (inherited from Phase 04 + Phase 05):
    - No `Liferay, Inc.` string anywhere. No real Liferay product / system names. No real teammate names — use placeholder roles like "a teammate", "the platform team", "a junior engineer on my team".
    - No real ticket prefixes (`LPD-`, `LPP-`, `LPS-`, `LRELEASE-`).
    - No emojis.
  </action>
  <verify>
    <automated>test -f docs/examples/monthly-report.md && test -f docs/examples/self-review.md && grep -q 'EXAMPLE-001' docs/examples/monthly-report.md && grep -q 'EXAMPLE-001' docs/examples/self-review.md && grep -q '## Theme(s) of the month' docs/examples/monthly-report.md && grep -q '^## 1. What have you accomplished' docs/examples/self-review.md && grep -q '^## 2. Since your last review' docs/examples/self-review.md && grep -q '^## 3. What is your current area of focus' docs/examples/self-review.md && grep -q 'Produce Excellence' docs/examples/self-review.md && grep -q 'Lead by Serving' docs/examples/self-review.md && grep -q 'Value People' docs/examples/self-review.md && grep -q 'Grow & Get Better' docs/examples/self-review.md && grep -q 'Stay Nerdy' docs/examples/self-review.md && grep -q '^## Sources' docs/examples/self-review.md && grep -q '^## Sources' docs/examples/monthly-report.md && grep -q 'fictional scrubbed example' docs/examples/monthly-report.md && grep -q 'fictional scrubbed example' docs/examples/self-review.md && ! grep -qE '\b(LPD|LPP|LPS|LRELEASE)-[0-9]+\b' docs/examples/monthly-report.md && ! grep -qE '\b(LPD|LPP|LPS|LRELEASE)-[0-9]+\b' docs/examples/self-review.md && ! grep -qi 'liferay, inc' docs/examples/monthly-report.md && ! grep -qi 'liferay, inc' docs/examples/self-review.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/examples/monthly-report.md` and `test -f docs/examples/self-review.md` both succeed.
    - `head -1 docs/examples/monthly-report.md` and `head -1 docs/examples/self-review.md` are both the opening `<!--` of the per-file disclaimer.
    - `grep -q '^# Monthly Report — ' docs/examples/monthly-report.md` succeeds.
    - `grep -q '^## Theme(s) of the month' docs/examples/monthly-report.md` succeeds (D-EXAMPLES-CONTINUITY — theme structure shows continuation across weeks).
    - `grep -q '^## Notable architectural decisions' docs/examples/monthly-report.md` succeeds.
    - `grep -q '^## Process / tooling improvements' docs/examples/monthly-report.md` succeeds.
    - `grep -q '^## Lessons learned' docs/examples/monthly-report.md` succeeds.
    - `grep -q '^## Risks / carry-over' docs/examples/monthly-report.md` succeeds.
    - `grep -q '^## Quick metrics' docs/examples/monthly-report.md` succeeds.
    - `grep -q '^## Sources' docs/examples/monthly-report.md` succeeds (final aggregated Sources block).
    - `grep -q '^### Weekly reports' docs/examples/monthly-report.md` succeeds (Sources grouped by type).
    - `grep -q '^# Self-Review — ' docs/examples/self-review.md` succeeds.
    - `grep -q 'YYYY-cycle1' docs/examples/self-review.md` succeeds (literal cycle placeholder per D-EXAMPLES-DATES).
    - `grep -q '^## 1. What have you accomplished since your last review' docs/examples/self-review.md` succeeds (Section 1 H2 verbatim per self-review prompt).
    - `grep -q '^## 2. Since your last review, what is something you would have done differently' docs/examples/self-review.md` succeeds (Section 2 H2 verbatim).
    - `grep -q '^## 3. What is your current area of focus as you "Grow & Get Better"' docs/examples/self-review.md` succeeds (Section 3 H2 verbatim).
    - All five Liferay values appear: `grep -q 'Produce Excellence' docs/examples/self-review.md`, `grep -q 'Lead by Serving' docs/examples/self-review.md`, `grep -q 'Value People' docs/examples/self-review.md`, `grep -q 'Grow & Get Better' docs/examples/self-review.md`, `grep -q 'Stay Nerdy' docs/examples/self-review.md` all succeed.
    - `grep -c '*(source:' docs/examples/self-review.md` is at least 5 (Section 1 + Section 2 + Section 3 sub-bullets all carry source italics).
    - `grep -c '^- \*\*' docs/examples/self-review.md` is at least 3 (Section 1 accomplishments use the `- **<accomplishment>**` bold-lead format).
    - `grep -q '^## Sources' docs/examples/self-review.md` succeeds and `grep -q '^### Monthly reports' docs/examples/self-review.md` succeeds (final Sources block grouped by type).
    - `grep -q 'EXAMPLE-001' docs/examples/self-review.md` and `grep -q 'EXAMPLE-001' docs/examples/monthly-report.md` both succeed (storyline continuity across all four files).
    - `grep -E '\b(LPD|LPP|LPS|LRELEASE)-[0-9]+\b' docs/examples/monthly-report.md docs/examples/self-review.md` returns no matches.
    - `grep -i 'liferay, inc' docs/examples/monthly-report.md docs/examples/self-review.md` returns no matches.
    - `grep -q 'fictional scrubbed example' docs/examples/monthly-report.md` and `grep -q 'fictional scrubbed example' docs/examples/self-review.md` both succeed.
  </acceptance_criteria>
  <done>All four `docs/examples/*.md` files exist. The four files share the EXAMPLE-NNN ticket set, present a coherent fictional engineer's narrative across day→week→month→cycle, mirror the shape of the real prompts, and pass every leakage-prevention grep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public-grade artifact | The `docs/examples/*.md` files are reference artifacts a Liferay engineer reads before deciding to install. The repo is private to `liferay-appsec/` today but Apache 2.0 is forward-compatible with going public — any leaked content compounds in risk. |
| author's vault → committed file | The original failure mode for this kind of work is "scrub a real vault, miss a real ticket title". D-EXAMPLES-SOURCE eliminates this by construction: no real-vault material is copied in at all. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Information disclosure) | `docs/examples/daily-log.md` | mitigate | Synthesized from scratch per D-EXAMPLES-SOURCE. Acceptance criteria run `grep -E '\b(LPD\|LPP\|LPS\|LRELEASE)-[0-9]+\b'` and require zero matches — any real Liferay ticket prefix in the file fails the check. EXAMPLE-NNN prefix is the only allowed ticket form (D-EXAMPLES-PREFIX). |
| T-05-02 | I (Information disclosure) | `docs/examples/weekly-report.md`, `docs/examples/monthly-report.md`, `docs/examples/self-review.md` | mitigate | Same synthesized-from-scratch invariant. Acceptance criteria require the same prefix-grep to return zero matches on each file. No real PR titles or commit hashes — every PR ref is invented and self-consistent with the daily-log's invented activity. |
| T-05-03 | I (Information disclosure) | `docs/examples/self-review.md` (Section 1 accomplishments) | mitigate | First-person voice forbids real teammate names: action explicitly instructs "use placeholder roles like 'a teammate', 'the platform team'". Acceptance criterion `grep -i 'liferay, inc'` returns no matches; the Phase-04 D-LEG-01-OVERRIDE no-`Liferay, Inc.`-anywhere rule applies here too. Real-person-name detection is not grep-automatable, but the synthesized-from-scratch construction means there are no real names to leak in the first place. |
| T-05-04 | T (Tampering) | per-file HTML-comment disclaimer | mitigate | Every file opens with the fictional-example disclaimer per D-EXAMPLES-SELF-DESCRIBE. Acceptance criteria check `grep -q 'fictional scrubbed example'` on each file. If a future executor strips the disclaimer ("looks cleaner without it"), the grep fails — disclaimer is load-bearing for distinguishing fictional from real even in raw view. |
| T-05-05 | I (Information disclosure) | EXAMPLE-NNN ticket numbers | accept | EXAMPLE-NNN is unambiguously fictional (D-EXAMPLES-PREFIX rejected `LPD-99xxx` precisely to avoid the brief-mistaken-for-real risk). No mitigation needed beyond the prefix choice. |
| T-05-06 | I (Information disclosure) | placeholder dates (YYYY-MM-DD, YYYY-Www, etc.) | accept | Literal-placeholder dates (D-EXAMPLES-DATES) cannot be a vector for leaking when the author was active because the dates are never concrete in the first place. No mitigation beyond the placeholder convention. |
</threat_model>

<verification>
After both tasks complete:
1. `ls docs/examples/` returns exactly four files: `daily-log.md`, `monthly-report.md`, `self-review.md`, `weekly-report.md`.
2. Every file passes the disclaimer + no-real-prefix + no-`Liferay, Inc.` greps from its task's acceptance criteria.
3. EXAMPLE-001 appears in all four files (storyline continuity check):
   `for f in docs/examples/*.md; do grep -q 'EXAMPLE-001' "$f" || echo "missing: $f"; done` prints nothing.
4. Each shape grep matches its file (`Theme of the week` in weekly; `Theme(s) of the month` in monthly; the three numbered review-question H2s in self-review).
</verification>

<success_criteria>
- The four `docs/examples/*.md` files exist and pass every grep acceptance criterion in Task 1 and Task 2.
- The four files form one coherent storyline anchored on EXAMPLE-001 / EXAMPLE-002 / EXAMPLE-003 — the daily-log's notes are the evidence for the weekly's bullets; the weekly's themes recur in the monthly's theme blocks; the monthly is one of the sources the self-review cites.
- Zero leakage: no real Liferay ticket prefix, no `Liferay, Inc.` string, no real PR title, no real teammate name in any file.
- Each file opens with the per-file fictional-example HTML-comment disclaimer.
- DOCS-05 is satisfied: `docs/examples/` contains four scrubbed reference artifacts (daily log, weekly report, monthly report, self-review draft) ready for the Wave-2 README rewrite to link to.
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-grade-documentation/05-01-SUMMARY.md` documenting: files created, lines per file, the EXAMPLE-NNN tickets used and the storyline arc, any deviations from the locked decisions in 05-CONTEXT.md (there should be none — flag any that emerge so Wave 2 can re-plan).
</output>
