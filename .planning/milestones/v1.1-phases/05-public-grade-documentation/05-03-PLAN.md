---
plan_id: 05-03
phase: 05-public-grade-documentation
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "CLAUDE.md §\"Testing locally\" no longer claims there is no test suite — it accurately reflects the 240-test suite that runs via `npm test`."
    - "A fresh contributor following CONTRIBUTING.md → CLAUDE.md no longer gets the conflicting signal that contributors must add tests for new features but that no test suite exists."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate Testing-locally section that matches v1.0+ reality"
      contains: "npm test"
  key_links:
    - from: "CLAUDE.md §Testing locally"
      to: "package.json#scripts.test"
      via: "the documented command matches the package.json script (`node --test test/*.test.js`)"
      pattern: "npm test"
---

<objective>
Fix the stale "no test suite yet (v0.1)" line in CLAUDE.md §"Testing locally". The line is wrong as of v1.0 close: the repo has 15 test files and 240 passing tests (run via `npm test`). Phase 04's CONTEXT.md explicitly handed this fix to Phase 05 — "Phase 05 fixes it" — and Phase 05's CONTEXT.md `<decisions>` Claude's Discretion bullet confirms it's in scope for this milestone so contributors landing in CONTRIBUTING.md → CLAUDE.md don't get conflicting signals.

This is a small, orthogonal change (one section of one file). It runs in Wave 2 in parallel with Plan 05-02 (README rewrite). Plan 05-02 touches `README.md` only; this plan touches `CLAUDE.md` only — no file overlap.

Purpose: Restore CLAUDE.md as a truthful architectural contract for contributors arriving via CONTRIBUTING.md.

Output: A revised `CLAUDE.md` §"Testing locally" that names the real test command, the real test layout, and the real test count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-public-grade-documentation/05-CONTEXT.md
@.planning/phases/04-legal-contributor-onboarding/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@CONTRIBUTING.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CLAUDE.md §"Testing locally" with an accurate test-bar paragraph</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the file being edited — §"Testing locally" starts at the line `## Testing locally` and ends at the next H2 or end-of-file; the current opening sentence is "There is no test suite yet (v0.1).")
    - .planning/phases/05-public-grade-documentation/05-CONTEXT.md (Claude's Discretion section — the locked correction wording: `npm test` runs `node --test test/*.test.js`, ~240 tests pass on v1.0 close)
    - .planning/phases/04-legal-contributor-onboarding/04-CONTEXT.md (`<deferred>` section: "CLAUDE.md staleness cleanup ... Phase 05 fixes it" — confirms scope handoff)
    - CONTRIBUTING.md (Phase 04 output — the contributor surface that points readers at CLAUDE.md and at `npm test`. The CLAUDE.md fix must not contradict CONTRIBUTING.md's test-bar wording)
    - package.json (`scripts.test` line — the literal command that must be named in CLAUDE.md)
  </read_first>
  <action>
    Edit `CLAUDE.md`. Find the section `## Testing locally`. Replace its opening sentence (currently "There is no test suite yet (v0.1). Verify by:") and the surrounding context, but **preserve the six-item verification checklist that follows** — items 1 through 6 of the existing Testing-locally section are still accurate (npm link, init with --no-set-default, session open, note, session close --hard, report --dry-run) and describe a real manual smoke check that complements the automated test suite.

    The new shape of `## Testing locally`:

    ```
    ## Testing locally

    Tests live under `test/` and run with `npm test` (which executes `node --test test/*.test.js`). The suite covers config, format, log-parser, logger, nudge, paths, session, state, stop-detector, topics, metrics, report-month, reviews, self-review, and cycles — 15 test files, 240 tests on v1.0 close.

    For changes that benefit from end-to-end manual verification (hook integration, the init flow, real Claude Code session lifecycle), run:

    1. `npm link` in this directory.
    2. `self-wiki init /tmp/test-vault --yes --no-set-default` — vault scaffolds, skill installs, hooks merge. **Always pass `--no-set-default` for tmp/test vaults** — without it, `init` rewrites `~/.config/self-wiki/config.json#vaultPath` globally and breaks the user's real-vault session lifecycle (this bit Phase 1's acceptance harness).
    3. `self-wiki session open --cwd /some/git/repo` then `self-wiki status` — should report active.
    4. `self-wiki note "test note"` — appends to today's `Daily/<date>.md`.
    5. `self-wiki session close --hard` — closes block, folds into `Tickets/<id>.md` if branch had a ticket.
    6. `self-wiki report --week <YYYY-Www> --dry-run` — prints the synthesis prompt.

    The plan that drove this implementation is at `~/.claude/plans/during-last-week-i-foamy-octopus.md`.
    ```

    Key constraints:
    - **First substantive line of the section** must reference `npm test` and `node --test test/*.test.js` (matches the `package.json#scripts.test` script).
    - **The `(v0.1)` parenthetical and the "There is no test suite yet" claim are gone.** No replacement parenthetical (no `(v1.0)`, no `(as of v1.1)`) — just current state.
    - **The 15-file / 240-test count** is grounded fact (`ls test/*.test.js | wc -l` = 15; `npm test` reports 240). Use those numbers; do not approximate.
    - **The manual six-step verification list stays** — it's complementary to the test suite, not redundant. Re-frame it as "end-to-end manual verification" rather than the only test path.
    - **The final pointer line** ("The plan that drove this implementation…") stays — it's still accurate provenance.
    - **No other section of CLAUDE.md is touched.** Architectural rules, repo layout, patterns, what-not-to-do all stay byte-for-byte.

    No emojis. No `Liferay, Inc.`.
  </action>
  <verify>
    <automated>grep -q '## Testing locally' CLAUDE.md && ! grep -q 'There is no test suite yet' CLAUDE.md && ! grep -q '(v0.1)' CLAUDE.md && grep -q 'npm test' CLAUDE.md && grep -q 'node --test test/' CLAUDE.md && grep -q '240 tests' CLAUDE.md && grep -q '15 test files' CLAUDE.md && grep -q 'self-wiki init /tmp/test-vault --yes --no-set-default' CLAUDE.md && grep -q 'foamy-octopus' CLAUDE.md && ! grep -qi 'liferay, inc' CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Testing locally$' CLAUDE.md` succeeds (the section heading is unchanged).
    - `grep -q 'There is no test suite yet' CLAUDE.md` returns nothing (the stale sentence is gone).
    - `grep -q '(v0.1)' CLAUDE.md` returns nothing (the stale version tag is gone).
    - `grep -q 'npm test' CLAUDE.md` succeeds (the real command is named).
    - `grep -q 'node --test test/' CLAUDE.md` succeeds (the underlying command from `package.json#scripts.test` is named).
    - `grep -q '240 tests' CLAUDE.md` succeeds (real count, grounded in current state).
    - `grep -q '15 test files' CLAUDE.md` succeeds (real count of `ls test/*.test.js`).
    - `grep -q 'self-wiki init /tmp/test-vault --yes --no-set-default' CLAUDE.md` succeeds (the six-step manual list is preserved).
    - `grep -q 'foamy-octopus' CLAUDE.md` succeeds (the trailing provenance line is preserved).
    - `grep -i 'liferay, inc' CLAUDE.md` returns nothing.
    - The four architectural rules section is unchanged: `grep -c '^## Architectural rules$' CLAUDE.md` returns 1, and `grep -c 'autonomy boundary is the hook' CLAUDE.md` returns at least 1.
    - The repo-layout fenced block is unchanged (sanity check): `grep -q 'core/' CLAUDE.md && grep -q 'commands/' CLAUDE.md && grep -q 'utils/' CLAUDE.md` succeeds.
    - The "What NOT to do" section is unchanged: `grep -q '^## What NOT to do$' CLAUDE.md` succeeds.
  </acceptance_criteria>
  <done>CLAUDE.md §"Testing locally" reflects the real test suite (npm test → node --test → 240 tests in 15 files). The six-step manual verification list is preserved. No other section of CLAUDE.md is touched. A contributor reading CONTRIBUTING.md → CLAUDE.md now gets consistent signals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor → CLAUDE.md | CLAUDE.md is the architectural contract CONTRIBUTING.md (Phase 04) points contributors at. Stale claims here propagate trust failures: a contributor told "new features need tests" while reading "there is no test suite yet" has no idea what bar they're meeting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | I (Information disclosure / accuracy) | CLAUDE.md §"Testing locally" | mitigate | Replace the stale "no test suite yet" sentence with the grounded fact (`npm test`, 15 files, 240 tests). Acceptance criteria grep for both the absence of the stale string and the presence of the real numbers. |
| T-05-14 | T (Tampering — accidental) | other CLAUDE.md sections | mitigate | Action scope explicitly limited to §"Testing locally"; acceptance criteria sanity-check the four architectural rules section, the repo-layout fence, and the What-NOT-to-do section are still present. If a future editor rewrites more than intended, the sanity-check greps fail. |
| T-05-15 | I (Information disclosure) | accidental introduction of `Liferay, Inc.` | mitigate | The Phase 04 D-LEG-01-OVERRIDE no-`Liferay, Inc.`-anywhere rule applies to CLAUDE.md too. Acceptance criterion `grep -i 'liferay, inc' CLAUDE.md` returns nothing. |
</threat_model>

<verification>
After Task 1 completes:
1. `grep -q 'There is no test suite yet' CLAUDE.md` returns nothing.
2. `grep -q 'npm test' CLAUDE.md && grep -q 'node --test test/' CLAUDE.md && grep -q '240 tests' CLAUDE.md && grep -q '15 test files' CLAUDE.md` succeeds.
3. The Architectural rules / repo-layout / patterns / what-not-to-do sections are byte-identical to their pre-task state (manual diff acceptable; acceptance criteria sanity-check the H2s and one signature string from each section).
4. `npm test` still runs to 240 passing tests (regression check — this plan only edits markdown but it doesn't hurt to confirm).
</verification>

<success_criteria>
- CLAUDE.md §"Testing locally" no longer claims there is no test suite.
- The accurate test-bar is documented: `npm test` → `node --test test/*.test.js` → 15 files, 240 tests on v1.0 close.
- The six-step manual verification list is preserved (it complements the automated suite for hook-integration / init-flow / real-Claude-Code-session work that the test suite can't cover).
- No other CLAUDE.md section is touched.
- No `Liferay, Inc.` introduced.
- The Phase 04 → Phase 05 carry-forward staleness fix is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-grade-documentation/05-03-SUMMARY.md` documenting: the before/after diff of §"Testing locally", the test-count source (the latest `npm test` run that produced 240), confirmation that no other CLAUDE.md section was touched, any deviations (there should be none).
</output>
