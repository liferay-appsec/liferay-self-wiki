---
plan_id: 05-02
phase: 05-public-grade-documentation
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - README.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04]
must_haves:
  truths:
    - "A first-time reader opening README.md sees the value prop (What you get) before the install instructions — demo-first inversion of today's install-first ordering."
    - "The install section uses the real `liferay-appsec/liferay-self-wiki` clone URL; the `<this-repo-url>` placeholder is gone from the file."
    - "The README contains three inline `**Example output:**` snippets (daily log, weekly report, self-review) of 10-15 lines each, followed by a `[→ Full example: docs/examples/<file>.md](docs/examples/<file>.md)` link line."
    - "The README's monthly-report subsection has a paragraph + link to `docs/examples/monthly-report.md` but NO inline snippet (D-SNIPPET-MONTHLY)."
    - "The README's 'What gets logged in your vault' section enumerates Captured items (branch names, ticket IDs, `self-wiki note` text, force-push counts, PR titles only-if-`gh`, JIRA ticket titles only-if-JIRA) and Not-captured items (file diffs, prompts, AI responses), plus a one-line scrub pointer and a `**Nothing leaves your machine automatically.**` paragraph."
    - "The README ends at Upgrading — no Support / Feedback section (D-NO-P07-STUB; Phase 07 appends)."
    - "The README links to LICENSE, NOTICE, and CONTRIBUTING.md (Phase 04 carry-forward)."
  artifacts:
    - path: "README.md"
      provides: "Demo-first public-grade README"
      contains: "What you get"
    - path: "README.md"
      provides: "Real repo URL in install section"
      contains: "liferay-appsec/liferay-self-wiki"
    - path: "README.md"
      provides: "Three Example output snippets + monthly link"
      contains: "**Example output:**"
    - path: "README.md"
      provides: "Privacy posture section"
      contains: "**Nothing leaves your machine automatically.**"
  key_links:
    - from: "README.md"
      to: "docs/examples/daily-log.md"
      via: "[→ Full example: docs/examples/daily-log.md](docs/examples/daily-log.md) link below the daily snippet"
      pattern: "\\[→ Full example: docs/examples/daily-log\\.md\\]"
    - from: "README.md"
      to: "docs/examples/weekly-report.md"
      via: "link below the weekly snippet"
      pattern: "\\[→ Full example: docs/examples/weekly-report\\.md\\]"
    - from: "README.md"
      to: "docs/examples/monthly-report.md"
      via: "link in the monthly paragraph (no inline snippet)"
      pattern: "docs/examples/monthly-report\\.md"
    - from: "README.md"
      to: "docs/examples/self-review.md"
      via: "link below the self-review snippet"
      pattern: "\\[→ Full example: docs/examples/self-review\\.md\\]"
    - from: "README.md"
      to: "LICENSE"
      via: "License pointer near end of README"
      pattern: "\\(LICENSE\\)"
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "CONTRIBUTING pointer (Install or Upgrading area)"
      pattern: "\\(CONTRIBUTING\\.md\\)"
    - from: "README.md"
      to: "NOTICE"
      via: "NOTICE pointer near LICENSE pointer"
      pattern: "\\(NOTICE\\)"
---

<objective>
Rewrite `README.md` demo-first: keep the existing 2-paragraph hook (lines 1-5) verbatim, then reorder everything else per D-README-ORDER so a Liferay engineer sees the value prop (What you get) and the privacy posture (What gets logged in your vault) BEFORE the install instructions. Replace the `<this-repo-url>` placeholder with the real `liferay-appsec/liferay-self-wiki` URL. Add three inline `**Example output:**` snippets pointing at the canonical files from Plan 05-01. Add the privacy-posture section. Wire links to LICENSE, NOTICE, and CONTRIBUTING.md (the Phase 04 carry-forward).

Purpose: Satisfy DOCS-01 (demo-first ordering), DOCS-02 (real URL), DOCS-03 (Example output snippets + links), DOCS-04 (privacy posture section), and the Phase 04 → Phase 05 link-wiring carry-forward.

Output: One rewritten `README.md` that passes every literal-string acceptance check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-public-grade-documentation/05-CONTEXT.md
@.planning/phases/04-legal-contributor-onboarding/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@README.md
@docs/examples/daily-log.md
@docs/examples/weekly-report.md
@docs/examples/monthly-report.md
@docs/examples/self-review.md
@src/core/logger.js
@src/core/topics.js
@src/core/detect.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure README — preserve hook, reorder sections per D-README-ORDER, replace `<this-repo-url>`, add demo-first body</name>
  <files>README.md</files>
  <read_first>
    - README.md (current — the file being rewritten; lines 1-5 stay verbatim per D-README-HOOK)
    - .planning/phases/05-public-grade-documentation/05-CONTEXT.md (full file — D-README-ORDER, D-README-HOOK, D-NO-P07-STUB, D-SNIPPET-* locks)
    - .planning/phases/04-legal-contributor-onboarding/04-CONTEXT.md (D-LEG-01-OVERRIDE: no `Liferay, Inc.` anywhere — this applies to README copyright/about lines too)
  </read_first>
  <action>
    Replace `README.md` with a demo-first restructuring. Approach: build the new file in section-order, preserve the existing prose where the new ordering permits, drop nothing material from the current file (Configuration, Parallel sessions, Upgrading sections keep their existing depth — D-README-ORDER only reorders; per `<code_context>` Reusable Assets the dense reference sections "stay as-is").

    **Step A — preserve the hook verbatim.** Keep lines 1-5 of the current README byte-for-byte:
    ```
    # self-wiki

    A self-writing personal wiki for engineers. Just run `claude` inside a repo — your work is captured into daily logs, topic pages, and weekly reports automatically. Zero per-session commands.

    The model: Claude Code hooks frame sessions; a skill instructs Claude to drop terse decision/outcome notes during work; a CLI synthesizes weekly reports and rebuilds topic pages on demand.
    ```

    **Step B — Emit the new section order per D-README-ORDER exactly.** Top-level (`##`) headings in this order, no others, nothing in between besides the hook above and a blank line:
      1. `## What you get` (DOCS-03 lands here — Task 2 fills the inline snippets)
      2. `## What gets logged in your vault` (DOCS-04 lands here — Task 3 fills it)
      3. `## Install`
      4. `## How sessions get framed`
      5. `## Daily commands`
      6. `## Weekly reports`
      7. `## Topic pages`
      8. `## Optional integrations`
      9. `## Configuration`
      10. `## Parallel Claude Code sessions`
      11. `## Upgrading`
      12. `## License` (small section — links to LICENSE + NOTICE, satisfies the Phase 04 carry-forward; D-NO-P07-STUB still applies — no Support / Feedback section; Phase 07 will append it AFTER License is fine, or License stays last — pick "License last" for stable Phase 07 append behavior)

    File ends at `## License`. **DO NOT** add Support / Feedback, Troubleshooting, FAQ, Acknowledgements, Roadmap, or any other section. Phase 07 (Support / Feedback) and Phase 06 (Troubleshooting) own their sections.

    **Step C — Replace the placeholder.** The current Install section contains `git clone <this-repo-url> self-wiki`. Replace `<this-repo-url>` with `https://github.com/liferay-appsec/liferay-self-wiki.git` (full HTTPS URL — clone-friendly, GitHub-renderable, and satisfies DOCS-02). The `<this-repo-url>` substring MUST NOT appear anywhere in the rewritten file.

    **Step D — Carry forward the existing section bodies.** For Install, How sessions get framed, Optional integrations, Daily commands, Weekly reports, Topic pages, Configuration, Parallel Claude Code sessions, Upgrading — preserve the existing prose / tables / code fences from the current README, modulo:
    - Reorder per D-README-ORDER.
    - The `<this-repo-url>` → real-URL replacement in Install.
    - Inside Install, add ONE additional pointer line — a single sentence + link pointing at `CONTRIBUTING.md` for contributors who want to work on the tool itself. Suggested wording: "Contributors: see [CONTRIBUTING.md](CONTRIBUTING.md) for the dev flow (`npm link`) and the test bar." Keep this to one line; do not duplicate CONTRIBUTING.md content.
    - The "What you get" block currently in the README (which is just a directory tree) gets MOVED into the new `## What you get` section as the FIRST sub-block before the per-artifact subsections that Task 2 will add. Update the directory tree if it uses real-looking dates (`2026-04-27.md`, `LPD-12345.md`) to keep parity with the placeholder convention — substitute `Daily/YYYY-MM-DD.md`, `Reports/YYYY-Www.md`, `Tickets/EXAMPLE-NNN.md`, `Components/<slug>.md` so the README stays consistent with the `docs/examples/` placeholder discipline. Add `Reviews/<YYYY>-cycle<N>.md` (it's been part of the vault since Phase 1 of v1.0).

    **Step E — License section content.** Final section, kept terse:
    ```
    ## License

    Apache 2.0. See [LICENSE](LICENSE) for the license text and [NOTICE](NOTICE) for copyright attribution.
    ```
    No `Liferay, Inc.` string anywhere in this section (D-LEG-01-OVERRIDE from Phase 04 — copyright attribution is "The self-wiki authors" per the NOTICE file).

    **Step F — leave the `## What you get` body and the `## What gets logged in your vault` body as placeholder stubs.** Each section emits its H2 heading plus a one-line `<!-- filled in by 05-02 Task 2 -->` / `<!-- filled in by 05-02 Task 3 -->` HTML comment placeholder. Tasks 2 and 3 will fill them in. The stubs let this task complete with a valid-but-incomplete README that already has the right section structure.

    Conventions to honor (from `.planning/codebase/CONVENTIONS.md` and CLAUDE.md): no emojis (the ✅ / ⚠️ in fenced examples inherited from `src/core/logger.js` are allowed because they are part of the file format being displayed); fenced code blocks for shell snippets; tables for reference where the existing README already uses them.
  </action>
  <verify>
    <automated>head -5 README.md | grep -q '^# self-wiki$' && head -5 README.md | grep -q 'A self-writing personal wiki for engineers' && ! grep -q '<this-repo-url>' README.md && grep -q 'https://github.com/liferay-appsec/liferay-self-wiki' README.md && grep -q '^## What you get$' README.md && grep -q '^## What gets logged in your vault$' README.md && grep -q '^## Install$' README.md && grep -q '^## How sessions get framed$' README.md && grep -q '^## Daily commands$' README.md && grep -q '^## Weekly reports$' README.md && grep -q '^## Topic pages$' README.md && grep -q '^## Optional integrations$' README.md && grep -q '^## Configuration$' README.md && grep -q '^## Parallel Claude Code sessions$' README.md && grep -q '^## Upgrading$' README.md && grep -q '^## License$' README.md && grep -q '\[LICENSE\](LICENSE)' README.md && grep -q '\[NOTICE\](NOTICE)' README.md && grep -q '\[CONTRIBUTING\.md\](CONTRIBUTING\.md)' README.md && ! grep -qi 'liferay, inc' README.md && ! grep -q '^## Support' README.md && ! grep -q '^## Troubleshooting' README.md && bash -c 'order=$(grep -nE "^## " README.md | cut -d: -f2); echo "$order" | head -1 | grep -q "## What you get" && echo "$order" | tail -1 | grep -q "## License"'</automated>
  </verify>
  <acceptance_criteria>
    - `head -5 README.md | grep -q '^# self-wiki$'` succeeds (hook line 1 preserved).
    - `head -5 README.md | grep -q 'A self-writing personal wiki for engineers'` succeeds (hook paragraph 1 preserved).
    - `head -5 README.md | grep -q 'The model: Claude Code hooks frame sessions'` succeeds (hook paragraph 2 preserved).
    - `grep -c '<this-repo-url>' README.md` returns 0 (DOCS-02).
    - `grep -c 'liferay-appsec/liferay-self-wiki' README.md` is at least 1 (DOCS-02 real URL present).
    - The twelve top-level H2 headings appear in the exact order: `What you get`, `What gets logged in your vault`, `Install`, `How sessions get framed`, `Daily commands`, `Weekly reports`, `Topic pages`, `Optional integrations`, `Configuration`, `Parallel Claude Code sessions`, `Upgrading`, `License`. Verify with:
      `grep -nE '^## ' README.md` produces the headings in that order, with `## What you get` first and `## License` last.
    - `grep -q '\[LICENSE\](LICENSE)' README.md` succeeds (Phase 04 carry-forward).
    - `grep -q '\[NOTICE\](NOTICE)' README.md` succeeds (Phase 04 carry-forward).
    - `grep -q '\[CONTRIBUTING\.md\](CONTRIBUTING\.md)' README.md` succeeds (Phase 04 carry-forward — README points at CONTRIBUTING.md from somewhere in the Install/Upgrading band).
    - `grep -i 'liferay, inc' README.md` returns no matches (Phase 04 D-LEG-01-OVERRIDE).
    - `grep -q '^## Support' README.md` returns nothing (D-NO-P07-STUB — no Phase 07 stub).
    - `grep -q '^## Troubleshooting' README.md` returns nothing (Phase 06 owns this section).
    - The directory-tree block under `## What you get` uses `Daily/YYYY-MM-DD.md`, `Reports/YYYY-Www.md`, `Tickets/EXAMPLE-NNN.md` placeholders (not `2026-04-27.md` or `LPD-12345.md`). Verify: `grep -q 'Daily/YYYY-MM-DD.md' README.md && grep -q 'Tickets/EXAMPLE-NNN.md' README.md` succeeds.
    - The `## What you get` and `## What gets logged in your vault` sections each contain an HTML-comment stub line indicating Task 2 / Task 3 will fill them in. (Sanity check before downstream tasks fill them.)
  </acceptance_criteria>
  <done>README.md is restructured. Hook preserved, sections in the exact D-README-ORDER order, real URL in Install, links to LICENSE/NOTICE/CONTRIBUTING.md present, no Support/Troubleshooting/Liferay-Inc strings, stubs in place for Tasks 2 and 3.</done>
</task>

<task type="auto">
  <name>Task 2: Fill `## What you get` — directory tree + three Example output snippets + monthly link (DOCS-03)</name>
  <files>README.md</files>
  <read_first>
    - README.md (modified by Task 1 — has the stubbed `## What you get` section)
    - .planning/phases/05-public-grade-documentation/05-CONTEXT.md (D-WHAT-YOU-GET-SHAPE, D-SNIPPET-LEN, D-SNIPPET-STYLE, D-SNIPPET-LINK, D-SNIPPET-MONTHLY)
    - docs/examples/daily-log.md (Plan 05-01 output — pick the 10-15-line excerpt to inline)
    - docs/examples/weekly-report.md (Plan 05-01 output — pick the 10-15-line excerpt)
    - docs/examples/monthly-report.md (Plan 05-01 output — paragraph + link only, NO inline snippet per D-SNIPPET-MONTHLY)
    - docs/examples/self-review.md (Plan 05-01 output — pick the 10-15-line excerpt)
  </read_first>
  <action>
    Replace the `## What you get` stub with the full demo-first body per D-WHAT-YOU-GET-SHAPE.

    **Order of sub-blocks inside `## What you get`:**

    1. **One-paragraph lead-in** (existing prose that today sits under "What you get" — already preserved by Task 1's Step D — describes the directory tree to follow). One sentence is enough: "Inside the vault directory you chose, `init` creates:".

    2. **Directory tree** in a fenced code block — the placeholder version Task 1 emitted with `Daily/YYYY-MM-DD.md`, `Reports/YYYY-Www.md`, `Tickets/EXAMPLE-NNN.md`, `Components/<slug>.md`, `Reviews/<YYYY>-cycle<N>.md`, `.self-wiki/config.json`.

    3. **Four per-artifact subsections** at H3 (`###`) level, in this exact order per D-WHAT-YOU-GET-SHAPE: Daily log → Weekly report → Monthly report → Self-review draft.

    For **`### Daily log`**, **`### Weekly report`**, and **`### Self-review draft`** — apply the literal D-SNIPPET-STYLE format exactly:
    ```
    ### [Artifact name]

    [1-2-sentence prose: what this is, when it gets written, what it summarizes.]

    **Example output:**

    ```markdown
    [scrubbed 10-15-line excerpt with `...` for truncation]
    ```

    [→ Full example: docs/examples/[file].md](docs/examples/[file].md)
    ```

    Specifics per subsection:
    - **`### Daily log`**: Excerpt from `docs/examples/daily-log.md`. Pick 10-15 lines that show the session-block shape (header line `## Session N — Task: EXAMPLE-NNN — …`, `- Started: HH:MM`, one or two `- Note [HH:MM]:` lines, and either the close-out triplet (Ended/Duration/Completed) or the open sentinel `<!-- session-N-open -->`). Use `...` for truncation. Link: `[→ Full example: docs/examples/daily-log.md](docs/examples/daily-log.md)`.
    - **`### Weekly report`**: Excerpt from `docs/examples/weekly-report.md`. Pick 10-15 lines that show the Theme of the week H2 + the Ticket | Layer | Outcome table + one bullet from Notable architectural decisions. Use `...` for truncation. Link: `[→ Full example: docs/examples/weekly-report.md](docs/examples/weekly-report.md)`.
    - **`### Self-review draft`**: Excerpt from `docs/examples/self-review.md`. Pick 10-15 lines that show the Section 1 H2 + 2-3 accomplishments with the value-tag clause (so the reader sees the `- **<accomplishment>** — <Value>` shape). Use `...` for truncation. Link: `[→ Full example: docs/examples/self-review.md](docs/examples/self-review.md)`.

    For **`### Monthly report`** — per D-SNIPPET-MONTHLY: **NO inline snippet**. Just one paragraph + link. Suggested prose: "A themed synthesis at `Reports/<YYYY-MM>.md`, consumed by `self-wiki self-review` as the primary input for the cycle draft. See [→ Full example: docs/examples/monthly-report.md](docs/examples/monthly-report.md)." This is the locked shape; do not invent a snippet here.

    **Fence-tag lock**: every inline snippet's fenced block uses the literal language tag `markdown` per D-SNIPPET-STYLE (the prompt's `<context>` for that decision: "GitHub renders this fine and the syntax highlighting is light enough not to distract"). No callout blockquote (`> [!NOTE]`) wrappers — that combination with nested fences is the rejected anti-pattern.

    **Label lock**: the literal phrase `**Example output:**` appears in bold immediately above each of the three fenced snippets. Not "Sample:", not "Output:", not "Excerpt:" — the literal phrase, satisfying DOCS-03 success criterion 3 verbatim.

    No emojis. No `Liferay, Inc.` (the snippets come from Plan 05-01 files that already pass that grep).
  </action>
  <verify>
    <automated>grep -q '^## What you get$' README.md && ! grep -q '<!-- filled in by 05-02 Task 2 -->' README.md && grep -c '\*\*Example output:\*\*' README.md | grep -qE '^3$' && grep -q '^### Daily log$' README.md && grep -q '^### Weekly report$' README.md && grep -q '^### Monthly report$' README.md && grep -q '^### Self-review draft$' README.md && grep -q '\[→ Full example: docs/examples/daily-log\.md\](docs/examples/daily-log\.md)' README.md && grep -q '\[→ Full example: docs/examples/weekly-report\.md\](docs/examples/weekly-report\.md)' README.md && grep -q 'docs/examples/monthly-report\.md' README.md && grep -q '\[→ Full example: docs/examples/self-review\.md\](docs/examples/self-review\.md)' README.md && ! grep -q '^### Monthly report$.*\*\*Example output:\*\*' README.md</automated>
  </verify>
  <acceptance_criteria>
    - The Task 1 stub `<!-- filled in by 05-02 Task 2 -->` is gone from `## What you get`.
    - Exactly three `**Example output:**` literal labels appear in README.md (one each for Daily log, Weekly report, Self-review draft — none for Monthly report per D-SNIPPET-MONTHLY). Verify: `grep -c '\*\*Example output:\*\*' README.md` returns exactly `3`.
    - All four H3 artifact subsections exist in this order: `### Daily log`, `### Weekly report`, `### Monthly report`, `### Self-review draft`. Verify ordering: `grep -nE '^### (Daily log|Weekly report|Monthly report|Self-review draft)$' README.md` returns four lines whose line numbers are strictly ascending and whose section names occur in that order.
    - The three link lines exist with the literal arrow + path: `grep -q '\[→ Full example: docs/examples/daily-log\.md\](docs/examples/daily-log\.md)' README.md`, `grep -q '\[→ Full example: docs/examples/weekly-report\.md\](docs/examples/weekly-report\.md)' README.md`, and `grep -q '\[→ Full example: docs/examples/self-review\.md\](docs/examples/self-review\.md)' README.md` all succeed.
    - The monthly subsection links to `docs/examples/monthly-report.md` (with or without the arrow form; the arrow form is preferred). Verify: `grep -q 'docs/examples/monthly-report\.md' README.md` succeeds.
    - The monthly subsection has no `**Example output:**` immediately under it (D-SNIPPET-MONTHLY — no inline snippet). Verify by inspecting the lines between `### Monthly report` and `### Self-review draft` — they should contain no `**Example output:**` label.
    - Each of the three snippets is inside a fenced code block tagged `markdown` (D-SNIPPET-STYLE). Verify with `grep -B1 -A1 '\*\*Example output:\*\*' README.md` showing `\`\`\`markdown` opening the next fence (manual inspection acceptable here; the strict grep automated check is the count-of-3 above).
    - The directory tree under `## What you get` uses placeholder filenames (no `2026-04-27.md`, no `LPD-12345.md`). This was verified in Task 1's acceptance but stays a regression check here.
  </acceptance_criteria>
  <done>The `## What you get` section is fully populated. Three inline `**Example output:**` snippets exist (daily/weekly/self-review) with their full-example link lines; the monthly is a paragraph + link with no inline snippet; the section headings appear in the locked order.</done>
</task>

<task type="auto">
  <name>Task 3: Fill `## What gets logged in your vault` — Captured / Not captured + scrub pointer + "Nothing leaves your machine automatically." (DOCS-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (modified by Tasks 1 and 2 — has the stubbed `## What gets logged in your vault` section)
    - .planning/phases/05-public-grade-documentation/05-CONTEXT.md (D-PRIVACY-DEPTH, D-PRIVACY-LISTS, D-PRIVACY-EXTERNAL, D-PRIVACY-NOTE-ORIGIN)
    - src/core/logger.js (source of truth for what lands in the daily log — the Captured-list bullets must match what `appendNote`, `openSessionBlockAtomic`, `closeSessionBlock`, `appendSwitch`, `markActivity`, `closeOrphanedSentinels` actually write)
    - src/core/topics.js (what lands in topic pages — informs the Captured-list bullets, especially the "force-push counts" claim from REQUIREMENTS.md DOCS-04 — verify whether force-push detection is in `logger.js` or computed downstream in `src/core/metrics.js`/`src/commands/report.js`)
    - src/core/detect.js (source of truth for the PR-title and JIRA-title bullets — `tryGhPrView` is the `gh` integration, `tryJiraTitle` is the JIRA integration; both must be annotated `(only if <integration> is configured)`)
  </read_first>
  <action>
    Replace the `## What gets logged in your vault` stub with the full privacy-posture body per D-PRIVACY-DEPTH (minimum viable depth — two lists + scrub pointer + the "Nothing leaves your machine automatically." paragraph).

    Layout, top to bottom:

    1. **Optional one-sentence lead-in.** A short prose sentence orienting the reader, e.g. "self-wiki only ever writes into the vault directory you chose at `init` time. Here's exactly what lands in it."

    2. **`### Captured`** subsection. Bulleted list. Required bullets (the planner-verified set against `src/core/logger.js`, `src/core/topics.js`, `src/core/detect.js`):
       - Branch names (detected from `git rev-parse --abbrev-ref HEAD` per `src/core/detect.js`).
       - Ticket IDs matched by your configured `ticketRegex` (default `\b(LPD|LPP|LPS|LRELEASE)-\d+\b`, customizable in `.self-wiki/config.json`).
       - The text of every `self-wiki note "<text>"` line dropped during the session (written into the daily file as `- Note [HH:MM]: <text>` per `src/core/logger.js#appendNote`).
       - Session start/end times, durations, and completion status (`Completed: ✅` or `Interrupted: ⚠️`) per `src/core/logger.js`.
       - Switch lines when you change tasks mid-session (`- Switched: HH:MM → <newTask>` per `src/core/logger.js#appendSwitch`).
       - Force-push counts (derived deterministically from your `self-wiki note` lines that mention force-pushing; surfaced in the weekly/monthly metrics blocks).
       - PR titles **(only if `gh` is configured and authenticated)** — fetched by `gh pr view --json title` per `src/core/detect.js#tryGhPrView`.
       - JIRA ticket titles **(only if JIRA is configured and `$JIRA_TOKEN` is exported)** — fetched via the JIRA REST API per `src/core/detect.js#tryJiraTitle`.

       The two integration-dependent bullets MUST end with the literal annotation `(only if <integration> is configured)` so a reader knows which items only apply when they opted in (D-PRIVACY-LISTS).

    3. **`### Not captured`** subsection. Bulleted list. Required bullets:
       - File diffs / patches / commit content (self-wiki never reads your working tree's file contents).
       - Your prompts to Claude (the conversation transcript is not parsed into the daily log; only the closing-summary tell detector inspects it, and that's transient — it doesn't persist transcript content).
       - Claude's responses (same — not parsed into the log).
       - Environment variables, shell history, or anything outside the vault directory.

    4. **One-line scrub pointer.** Single sentence telling a user how to scrub before sharing screenshots. Suggested wording: "Before sharing a screenshot, open the daily file and delete any note line you want to keep private — daily files are plain markdown."

    5. **`**Nothing leaves your machine automatically.**` paragraph** (D-PRIVACY-EXTERNAL). The heading sentence is bolded; the body that follows is not bolded. Content (per the lock):
       > **Nothing leaves your machine automatically.** self-wiki itself makes no network calls. `claude -p` is invoked only when you explicitly run a synthesis command (`self-wiki report`, `self-wiki self-review`). `gh` and JIRA are read-only, opt-in, and only invoked during `session open` to enrich the detected task. Your daily logs never leave your vault unless you copy them out.

    D-PRIVACY-NOTE-ORIGIN: Do NOT mention that `self-wiki note` lines are typically dropped by Claude (via the wiki skill). The Captured-list bullet says "the text of every `self-wiki note "<text>"` line"; that's enough.

    Verify the lists are accurate against the actual implementation while you write them (D-PRIVACY-LISTS planner note: "verify the lists are accurate against the actual logger / topics implementation before committing"). If you find a Captured item the bullets miss — for example, if `src/core/logger.js#markActivity` writes something user-visible that the bullets don't enumerate — add it. The point of grounding the lists in the source files is to keep the public-grade claim honest.

    No emojis (the ✅/⚠️ inline glyphs that appear inside the close-line example are acceptable because they're inside backticks). No `Liferay, Inc.`.
  </action>
  <verify>
    <automated>grep -q '^## What gets logged in your vault$' README.md && ! grep -q '<!-- filled in by 05-02 Task 3 -->' README.md && grep -q '^### Captured$' README.md && grep -q '^### Not captured$' README.md && grep -q '\*\*Nothing leaves your machine automatically\.\*\*' README.md && grep -q 'branch name' README.md && grep -q 'ticket' README.md && grep -q 'self-wiki note' README.md && grep -q 'force-push' README.md && grep -q 'PR title' README.md && grep -q 'JIRA' README.md && grep -q 'file diff' README.md && grep -q 'prompt' README.md && grep -qE '\(only if .* is configured\)' README.md && grep -q 'claude -p' README.md && ! grep -qi 'liferay, inc' README.md</automated>
  </verify>
  <acceptance_criteria>
    - The Task 1 stub `<!-- filled in by 05-02 Task 3 -->` is gone from `## What gets logged in your vault`.
    - `grep -q '^### Captured$' README.md` succeeds.
    - `grep -q '^### Not captured$' README.md` succeeds.
    - The literal bolded label appears: `grep -q '\*\*Nothing leaves your machine automatically\.\*\*' README.md` succeeds.
    - The Captured list mentions branch names, ticket IDs, `self-wiki note` text, force-push counts. Verify with grep: `grep -q 'branch name' README.md`, `grep -q 'ticket' README.md`, `grep -q 'self-wiki note' README.md`, `grep -q 'force-push' README.md` all succeed.
    - The two integration-dependent bullets (PR titles and JIRA titles) appear and carry the `(only if <integration> is configured)` annotation literally. Verify: `grep -q 'PR title' README.md`, `grep -q 'JIRA' README.md`, and `grep -qE '\(only if .* is configured\)' README.md` all succeed. The annotation count should be at least 2.
    - The Not-captured list mentions file diffs, prompts, and AI responses. Verify: `grep -q 'file diff' README.md`, `grep -q 'prompt' README.md`, and at least one mention of "responses" or "answers" / `grep -qE '(response|answer)' README.md` succeed.
    - The paragraph under `**Nothing leaves your machine automatically.**` mentions `claude -p` and identifies the synthesis commands as the only times it runs. Verify: `grep -q 'claude -p' README.md` succeeds.
    - The paragraph mentions that `gh` and JIRA are opt-in / only invoked during `session open`. Verify: `grep -q 'session open' README.md` succeeds (the paragraph plus the existing How-sessions-get-framed table both reference it; only the paragraph membership matters for this criterion — manual confirmation acceptable).
    - The section uses minimum viable depth per D-PRIVACY-DEPTH — no "how to scrub" subsection with copy-paste commands, no before/after scrub mini-example. (Detection: section length under `## What gets logged in your vault` should be roughly 25-40 lines; if it exceeds 60 lines, suspect scope creep.)
    - No emojis used as decoration. No `Liferay, Inc.` string introduced. Verify: `grep -i 'liferay, inc' README.md` returns nothing.
  </acceptance_criteria>
  <done>The `## What gets logged in your vault` section is fully populated. Both lists are present and grounded in the implementation; the `**Nothing leaves your machine automatically.**` paragraph is the last thing in the section; DOCS-04 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public-grade artifact | `README.md` is the front door for any Liferay engineer arriving via the Phase 07 Slack post. Anything misleading or stale here propagates trust failures. |
| Phase 04 artifacts → README links | The Phase 04 CONTEXT.md D-LEG-01-OVERRIDE invariant ("no `Liferay, Inc.` anywhere") extends to README copyright / about lines. If a future executor "helpfully" re-introduces the company name, the legal posture from Phase 04 silently degrades. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | I (Information disclosure) | README inline `**Example output:**` snippets | mitigate | Snippets are excerpted from `docs/examples/*.md` files, which Plan 05-01 already proved leakage-free with prefix-grep and `Liferay, Inc.` grep. Task 2 acceptance criteria require the snippets to come from those files. No real-vault material can enter via this path. |
| T-05-08 | I (Information disclosure) | README copyright / about lines | mitigate | Phase 04 D-LEG-01-OVERRIDE: `grep -i 'liferay, inc' README.md` must return zero matches. Task 1 acceptance criteria enforce this. The `## License` section attributes via "the project / its authors" pattern (NOTICE file), not via the company name. |
| T-05-09 | T (Tampering) | README structural integrity | mitigate | D-README-ORDER lock is enforced via the section-order grep in Task 1 acceptance criteria. If a future contributor reshuffles sections, the grep regression catches it. |
| T-05-10 | S (Spoofing) | `<this-repo-url>` placeholder | mitigate | DOCS-02 satisfied by Task 1's replacement check. The `<this-repo-url>` placeholder failing to be replaced would let a fresh user clone from a non-existent URL, which is a small-S spoof of the install path. Acceptance criterion `grep -c '<this-repo-url>' README.md` returns 0 catches this. |
| T-05-11 | I (Information disclosure) | "What gets logged in your vault" lists | mitigate | D-PRIVACY-LISTS requires the Captured/Not-captured lists be verified against `src/core/logger.js`, `src/core/topics.js`, `src/core/detect.js`. Task 3 `<read_first>` mandates reading those source files. The integration-dependent bullets carry `(only if <integration> is configured)` so an opt-out reader knows the optional surface. False reassurance ("we don't capture X" when we do) is the failure mode this list-accuracy check exists to prevent. |
| T-05-12 | I (Information disclosure) | Phase 07 stub leakage | accept | D-NO-P07-STUB explicitly forbids pre-creating a Support / Feedback section. Risk is the inverse — a future executor "helpfully" adding a TODO placeholder. Acceptance criterion `grep -q '^## Support' README.md` returning nothing catches this. |
</threat_model>

<verification>
After all three tasks complete:
1. The H2 section order in `README.md` matches D-README-ORDER exactly. Verify:
   `grep -nE '^## ' README.md` produces 12 lines in this order: What you get, What gets logged in your vault, Install, How sessions get framed, Daily commands, Weekly reports, Topic pages, Optional integrations, Configuration, Parallel Claude Code sessions, Upgrading, License.
2. DOCS-01 (demo-first): `## What you get` appears before `## Install` (line number of first < line number of second).
3. DOCS-02: `grep -c '<this-repo-url>' README.md` returns 0 and `grep -c 'liferay-appsec/liferay-self-wiki' README.md` is ≥1.
4. DOCS-03: Exactly three `**Example output:**` labels (`grep -c '\*\*Example output:\*\*' README.md` returns `3`); each followed by a fenced `\`\`\`markdown` snippet and a `[→ Full example: docs/examples/*.md](...)` link.
5. DOCS-04: `### Captured` and `### Not captured` lists present; `**Nothing leaves your machine automatically.**` literal present; integration-dependent bullets carry `(only if <integration> is configured)`.
6. Phase 04 carry-forward: README links to LICENSE, NOTICE, and CONTRIBUTING.md (`grep -q '(LICENSE)' README.md`, `grep -q '(NOTICE)' README.md`, `grep -q '(CONTRIBUTING.md)' README.md`).
7. No leakage: `grep -i 'liferay, inc' README.md` returns nothing.
8. No premature Phase 07/06 sections: `grep -E '^## (Support|Feedback|Troubleshooting|FAQ)' README.md` returns nothing.
</verification>

<success_criteria>
- All 12 top-level H2 sections appear in the order locked by D-README-ORDER.
- DOCS-01 demo-first inversion confirmed: `## What you get` appears before `## Install`.
- DOCS-02 satisfied: `<this-repo-url>` is gone; `https://github.com/liferay-appsec/liferay-self-wiki.git` is in the Install fence.
- DOCS-03 satisfied: three `**Example output:**` snippets (daily / weekly / self-review) each 10-15 lines, each followed by a `[→ Full example: …](…)` link; monthly is link-only.
- DOCS-04 satisfied: Captured / Not-captured lists grounded in `src/core/logger.js` / `topics.js` / `detect.js`; integration-dependent items carry `(only if <integration> is configured)`; `**Nothing leaves your machine automatically.**` paragraph present.
- Phase 04 carry-forward closed: README links to LICENSE, NOTICE, CONTRIBUTING.md.
- No `Liferay, Inc.` string introduced anywhere in README.
- No Phase 06 (Troubleshooting) or Phase 07 (Support / Feedback) stubs added — they will land later, cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-grade-documentation/05-02-SUMMARY.md` documenting: section ordering before/after, the literal URL string used in Install, the three snippet line counts, the integration-dependent bullets' wording, any deviations from the locked decisions in 05-CONTEXT.md (there should be none — flag any that emerge).
</output>
