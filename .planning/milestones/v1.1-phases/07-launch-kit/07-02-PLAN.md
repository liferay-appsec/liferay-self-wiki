---
phase: 07
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - LAUNCH-02
must_haves:
  truths:
    - "A reader who lands on README.md from the Slack launch post finds an unambiguous `## Support / Feedback` section as the final section of the file, after `## License`."
    - "The Support / Feedback section names both surfaces with intent: Slack channel placeholder for questions, GitHub Issues URL for bugs / feature requests."
    - "The Slack channel placeholder string `#self-wiki-feedback (TODO: confirm channel name)` is byte-identical to the placeholder in `docs/launch-post.md` (plan 07-01)."
    - "The GitHub Issues URL `https://github.com/liferay-appsec/liferay-self-wiki/issues` matches CONTRIBUTING.md's URL (Phase 04 D-ISSUE-DEST) — a contributor crossing surfaces sees the same URL everywhere."
    - "The README section order remains as Phase 06 left it: `… → Upgrading → Troubleshooting → License → Support / Feedback` (D-README-PLACEMENT)."
  artifacts:
    - path: "README.md"
      provides: "Tail-appended `## Support / Feedback` section (LAUNCH-02)."
      contains: "## Support / Feedback"
  key_links:
    - from: "README.md `## Support / Feedback`"
      to: "Slack channel `#self-wiki-feedback (TODO: confirm channel name)`"
      via: "intent-prefixed line (questions)"
      pattern: "#self-wiki-feedback \\(TODO: confirm channel name\\)"
    - from: "README.md `## Support / Feedback`"
      to: "https://github.com/liferay-appsec/liferay-self-wiki/issues"
      via: "intent-prefixed line (bugs / feature requests)"
      pattern: "https://github.com/liferay-appsec/liferay-self-wiki/issues"
    - from: "docs/launch-post.md (feedback section, plan 07-01)"
      to: "README.md `## Support / Feedback`"
      via: "byte-identical Slack-channel placeholder string"
      pattern: "#self-wiki-feedback \\(TODO: confirm channel name\\)"
---

<objective>
Append a new `## Support / Feedback` section to `README.md` as the
final section of the file, after the existing `## License` section
(D-README-PLACEMENT). The section is a minimal two-line pointer
per D-README-SECTION-MINIMAL: one line names the Slack channel
placeholder (questions / quick chatter), one line names the GitHub
Issues URL (bugs / feature requests). Same surfaces and same
intent split as the launch post's feedback line (plan 07-01) — a
reader crossing between the two surfaces sees consistent guidance.

Purpose: ship LAUNCH-02. Phase 05 explicitly did not stub this
section (D-NO-P07-STUB); Phase 06 explicitly reserved the tail
position for Phase 07 (D-PLACEMENT). This plan closes the v1.1
README structure.

Output: one modified file — `README.md` — appended-only. No
section reorders, no edits to existing content above `## License`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-launch-kit/07-CONTEXT.md
@.planning/phases/04-legal-contributor-onboarding/04-CONTEXT.md
@.planning/phases/05-public-grade-documentation/05-CONTEXT.md
@.planning/phases/06-install-ux-hardening/06-CONTEXT.md
@README.md
@CONTRIBUTING.md

<interfaces>
<!--
The README is currently ~279 lines with `## License` as the final
section. Phase 07 appends `## Support / Feedback` below it. The
section's exact wording is locked verbatim in CONTEXT.md
<specifics>. Use it as-is — this is the cross-plan-contract string
that must match plan 07-01's feedback section.
-->

From `.planning/phases/07-launch-kit/07-CONTEXT.md` <specifics>
section, the verbatim section to append:

```markdown
## Support / Feedback

Quick chatter / questions: `#self-wiki-feedback (TODO: confirm
channel name)` on Liferay Slack.

Bugs / feature requests: https://github.com/liferay-appsec/liferay-self-wiki/issues.
```

Two lines, each carrying its intent in the opening clause, each
ending with the surface (Slack channel or GitHub Issues URL).
Per D-README-SECTION-MINIMAL.

From `CONTRIBUTING.md` lines 7–16 — the canonical GitHub Issues
URL the README must match (Phase 04 D-ISSUE-DEST):

```
Open a GitHub issue:
https://github.com/liferay-appsec/liferay-self-wiki/issues
```

These three surfaces (CONTRIBUTING.md + README.md + the launch
post from plan 07-01) all name the same URL with the same
parenthetical/intent — a contributor crossing between them never
sees a different destination.

From `README.md` lines 276–278 (the current tail — what plan 07-02
appends after):

```
## License

Apache 2.0. See [LICENSE](LICENSE) for the license text and [NOTICE](NOTICE) for copyright attribution.
```

The new section is appended after line 278. Insert exactly one
blank line between `## License`'s body and the new `## Support /
Feedback` header (standard markdown section spacing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the `## Support / Feedback` section to README.md after `## License`</name>
  <files>README.md</files>
  <read_first>
    - `README.md` (full file, but ESPECIALLY lines 266–278 — confirm the file currently ends with the License section and verify Phase 05 D-NO-P07-STUB held: no pre-existing Support / Feedback stub)
    - `.planning/phases/07-launch-kit/07-CONTEXT.md` (D-README-SECTION-MINIMAL, D-README-PLACEMENT, D-FEEDBACK-BOTH-SURFACES, D-FEEDBACK-PLACEHOLDER-VERBATIM, D-NO-EMOJIS — all locked)
    - `CONTRIBUTING.md` lines 7–16 (confirm the GitHub Issues URL the README appends matches CONTRIBUTING.md byte-for-byte — D-ISSUE-DEST carry-forward)
    - `.planning/phases/04-legal-contributor-onboarding/04-CONTEXT.md` (D-ISSUE-DEST, D-ISSUE-NOT-SLACK — note: Phase 07 does NOT add Slack to CONTRIBUTING.md; Slack lives only on the launch post and the README §Support/Feedback)
    - `.planning/phases/05-public-grade-documentation/05-CONTEXT.md` (D-NO-P07-STUB — the README does NOT pre-create this section; Phase 07 appends it cleanly with no TODO marker, no placeholder)
    - `.planning/phases/06-install-ux-hardening/06-CONTEXT.md` (D-PLACEMENT — final section order is `… → Upgrading → Troubleshooting → License → Support / Feedback`)
  </read_first>
  <action>
    Append a new section to the **end** of `README.md`. The new
    section is the **final** section of the file. Do NOT reorder
    any existing sections. Do NOT edit any content above
    `## License`.

    Per D-README-SECTION-MINIMAL + D-FEEDBACK-BOTH-SURFACES +
    D-FEEDBACK-PLACEHOLDER-VERBATIM + the CONTEXT.md <specifics>
    section, the appended section is **exactly this** (use this
    verbatim — the Slack placeholder and GitHub Issues URL strings
    are cross-plan contracts with plan 07-01 and with
    CONTRIBUTING.md):

    ```markdown
    ## Support / Feedback

    Quick chatter / questions: `#self-wiki-feedback (TODO: confirm channel name)` on Liferay Slack.

    Bugs / feature requests: https://github.com/liferay-appsec/liferay-self-wiki/issues.
    ```

    Format notes:
    - Section heading is exactly `## Support / Feedback` (two
      H2-level hashes, space-slash-space, capitalization as shown).
      The slash makes the dual purpose visible at a glance and
      matches the success-criterion text verbatim — recommended
      per CONTEXT.md "Claude's Discretion" guidance.
    - Two body lines (not bullets, not prose paragraph). Each
      line is `<intent>: <surface>.` shape. The placeholder line
      uses inline code backticks around `#self-wiki-feedback (TODO:
      confirm channel name)` so the # doesn't trigger a markdown
      heading (some renderers are jumpy about leading `#`); the
      Issues URL line uses a bare URL (no markdown link wrapping)
      so it's an unambiguous machine-grep target and stays
      consistent with CONTRIBUTING.md's bare-URL shape.
    - One blank line separates the new section from the previous
      `## License` body.
    - No emojis, no shortcodes, no exclamation marks (D-NO-EMOJIS
      carry-forward + Phase 05 voice).
    - No "TODO marker" outside the literal Slack placeholder
      parenthetical — D-NO-P07-STUB explicitly says this section
      ships clean. The `(TODO: confirm channel name)` parenthetical
      inside the inline-code Slack handle is the placeholder
      D-FEEDBACK-PLACEHOLDER-VERBATIM locks; it is NOT a Phase 07
      TODO on the section itself.

    Cross-plan-contract: the literal string `#self-wiki-feedback
    (TODO: confirm channel name)` MUST be byte-identical to the
    same string in `docs/launch-post.md` (plan 07-01). A simple
    grep across both files should match. Same goes for the GitHub
    Issues URL.

    Phase 07 does NOT modify `CONTRIBUTING.md`. D-ISSUE-NOT-SLACK
    (Phase 04 carry-forward) explicitly keeps CONTRIBUTING.md on
    GitHub Issues only — Slack lives on the README and the launch
    post.
  </action>
  <verify>
    <automated>
      bash -c 'F=README.md; \
        test -f "$F" || { echo "FAIL: README missing"; exit 1; }; \
        grep -cF "## Support / Feedback" "$F" | awk "{ if (\$1 != 1) { print \"FAIL: Support / Feedback header count\", \$1, \"!= 1\"; exit 1 } else { print \"OK: Support / Feedback header present exactly once\" } }" && \
        grep -E "^## " "$F" | tail -1 | grep -Fxq "## Support / Feedback" && echo "OK: Support / Feedback is the LAST H2 section" && \
        grep -cF "#self-wiki-feedback (TODO: confirm channel name)" "$F" | awk "{ if (\$1 != 1) { print \"FAIL: Slack placeholder count\", \$1, \"!= 1\"; exit 1 } else { print \"OK: Slack placeholder present exactly once\" } }" && \
        grep -cF "https://github.com/liferay-appsec/liferay-self-wiki/issues" "$F" | awk "{ if (\$1 < 1) { print \"FAIL: GitHub Issues URL missing from README\"; exit 1 } else { print \"OK: GitHub Issues URL present\", \$1, \"time(s) in README\" } }" && \
        awk "/^## License/{lic=NR} /^## Support \\/ Feedback/{sup=NR} END { if (lic > 0 && sup > lic) print \"OK: Support / Feedback (line\", sup, \") appears after License (line\", lic, \")\"; else { print \"FAIL: section ordering broken — License at\", lic, \"Support at\", sup; exit 1 } }" "$F" && \
        echo "--- Cross-plan-contract: launch post + README share the same strings ---" && \
        diff <(grep -F "#self-wiki-feedback (TODO: confirm channel name)" "$F" | head -1) <(grep -F "#self-wiki-feedback (TODO: confirm channel name)" docs/launch-post.md | head -1) >/dev/null && echo "OK: Slack placeholder identical across README + docs/launch-post.md (substring match)" || echo "NOTE: lines differ in surrounding prose (expected — they live in different sections); the bracketed substring matches both files (already verified above by grep -cF on each)." && \
        echo "ALL CHECKS PASSED"'
    </automated>
  </verify>
  <done>
    `README.md` ends with a `## Support / Feedback` section after
    `## License`. The section contains the Slack placeholder
    `#self-wiki-feedback (TODO: confirm channel name)` exactly
    once and the GitHub Issues URL
    `https://github.com/liferay-appsec/liferay-self-wiki/issues`
    at least once. The header `## Support / Feedback` is the
    LAST H2 in the file. No content above `## License` was
    modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-plan contract + non-regression check</name>
  <files></files>
  <read_first>
    - The terminal output from Task 1 (README appended correctly).
    - `docs/launch-post.md` (from plan 07-01 if executed in the same wave — the cross-plan-contract check below assumes this file exists; if 07-01 runs after 07-02 in the same wave, run this verify task again after both are done).
  </read_first>
  <action>
    Phase 07 is documentation-only — Task 1 modified exactly one
    existing file (`README.md`) by appending one new section.
    Confirm:

    1. **Cross-plan-contract.** The literal string
       `#self-wiki-feedback (TODO: confirm channel name)` MUST
       appear in BOTH `README.md` AND `docs/launch-post.md`
       (created by plan 07-01). The bracketed substring is the
       byte-identical placeholder. If plan 07-01 has not yet run
       in this wave (parallel execution), defer this check until
       both plans land; the verify command below counts both
       files.

    2. **CONTRIBUTING.md consistency.** The GitHub Issues URL
       (`https://github.com/liferay-appsec/liferay-self-wiki/issues`)
       in the appended section must match the same URL in
       `CONTRIBUTING.md` — verified by simple grep across both
       files (Phase 04 D-ISSUE-DEST carry-forward; one URL, three
       surfaces).

    3. **Non-regression.** Run `npm test` (257/257 from Phase 06
       must stay green) and `self-wiki --help` (CLI surface still
       prints).

    4. **CONTRIBUTING.md unchanged.** `git status CONTRIBUTING.md`
       must show no modifications — D-ISSUE-NOT-SLACK
       (Phase 04 carry-forward) explicitly keeps Slack out of
       CONTRIBUTING.md.
  </action>
  <verify>
    <automated>
      bash -c '\
        echo "--- Cross-plan-contract: Slack placeholder in BOTH files ---" && \
        grep -lF "#self-wiki-feedback (TODO: confirm channel name)" README.md docs/launch-post.md 2>/dev/null | wc -l | awk "{ if (\$1 == 2) print \"OK: both README.md and docs/launch-post.md contain the Slack placeholder\"; else { print \"FAIL: placeholder appears in\", \$1, \"of 2 expected files (note: if launch post plan 07-01 has not yet run, re-run after both plans land)\"; exit 1 } }" && \
        echo "--- CONTRIBUTING.md GitHub Issues URL parity ---" && \
        grep -lF "https://github.com/liferay-appsec/liferay-self-wiki/issues" README.md CONTRIBUTING.md docs/launch-post.md 2>/dev/null | wc -l | awk "{ if (\$1 == 3) print \"OK: GitHub Issues URL present in README + CONTRIBUTING + launch post\"; else { print \"FAIL: URL appears in\", \$1, \"of 3 expected files\"; exit 1 } }" && \
        echo "--- CONTRIBUTING.md untouched ---" && \
        ! git status --porcelain CONTRIBUTING.md | grep -q "^.M" && echo "OK: CONTRIBUTING.md unmodified (D-ISSUE-NOT-SLACK held)" && \
        echo "--- Non-regression ---" && \
        npm test 2>&1 | tail -20 | grep -E "tests [0-9]+|pass [0-9]+|# tests [0-9]+" && \
        (self-wiki --help 2>/dev/null || node src/cli.js --help) | head -1 | grep -F "Usage" && \
        echo "ALL CHECKS PASSED"'
    </automated>
  </verify>
  <done>
    The Slack placeholder `#self-wiki-feedback (TODO: confirm
    channel name)` appears in BOTH `README.md` and
    `docs/launch-post.md` (cross-plan-contract from plan 07-01
    held). The GitHub Issues URL
    `https://github.com/liferay-appsec/liferay-self-wiki/issues`
    appears consistently in `README.md` + `CONTRIBUTING.md` +
    `docs/launch-post.md`. `CONTRIBUTING.md` was not modified
    (D-ISSUE-NOT-SLACK held). `npm test` passes (257/257 from
    Phase 06 stays green). `self-wiki --help` still prints.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → README.md | This plan appends a documentation section. No runtime input crosses any boundary; the appended content is plain markdown displayed by GitHub's renderer to authenticated Liferay engineers browsing the internal `liferay-appsec/` repo. |

## STRIDE Threat Register

Phase 07 plan 02 is documentation-only: no new code paths, no new
commands, no new flags, no new permissions, no dependency changes,
no `src/` edits. The threat surface is negligible.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure | `README.md` `## Support / Feedback` section references `https://github.com/liferay-appsec/liferay-self-wiki/issues` | accept | The URL is already public in `CONTRIBUTING.md` (Phase 04 D-ISSUE-DEST) and is the canonical bug-tracking surface; not a secret. README appending is consistent with CONTRIBUTING.md and the launch post. |
| T-07-05 | Spoofing | Slack channel placeholder could be misread as a live channel name | mitigate | The literal `(TODO: confirm channel name)` parenthetical and the inline-code formatting around the channel handle make the placeholder unambiguous per D-FEEDBACK-PLACEHOLDER-VERBATIM. Success criterion 3 explicitly forbids placeholders that can be mistaken for live content. Verified by the grep checks in Task 1. |
| T-07-06 | Repudiation | A future contributor edits `## Support / Feedback` and silently changes the feedback channel | accept | Standard git history covers this. The section is two lines; any change is reviewable. Out of scope for Phase 07. |
</threat_model>

<verification>
- README.md contains `## Support / Feedback` exactly once.
- The `## Support / Feedback` header is the LAST H2 in the file.
- README.md contains `#self-wiki-feedback (TODO: confirm channel name)` exactly once (D-FEEDBACK-PLACEHOLDER-VERBATIM, byte-identical with plan 07-01).
- README.md contains `https://github.com/liferay-appsec/liferay-self-wiki/issues` at least once (D-FEEDBACK-BOTH-SURFACES, byte-identical with CONTRIBUTING.md).
- `## Support / Feedback` appears after `## License` (D-README-PLACEMENT).
- CONTRIBUTING.md was not modified (D-ISSUE-NOT-SLACK held — Phase 04 carry-forward).
- `npm test` still passes (257/257 from Phase 06).
- `self-wiki --help` still prints.
</verification>

<success_criteria>
LAUNCH-02 satisfied:
- A reader arriving at the README via the Slack launch post finds
  an unambiguous `## Support / Feedback` section as the final
  section, naming the Slack channel for questions and the GitHub
  Issues URL for bugs / feature requests (success criterion 3).
- The Slack channel name `#self-wiki-feedback (TODO: confirm
  channel name)` is unmistakably a placeholder (success
  criterion 3 explicit shape match).
- The 257-test suite from Phase 06 stays green and
  `self-wiki --help` still prints (non-regression).
- The cross-plan contract holds: the Slack placeholder string
  and the GitHub Issues URL appear identically in `README.md`,
  `docs/launch-post.md`, and (for the URL only) `CONTRIBUTING.md`.
</success_criteria>

<output>
After completion, create
`.planning/phases/07-launch-kit/07-02-SUMMARY.md` per the
get-shit-done summary template.
</output>
