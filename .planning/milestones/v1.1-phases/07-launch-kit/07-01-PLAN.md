---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/launch-post.md
autonomous: true
requirements:
  - LAUNCH-01
must_haves:
  truths:
    - "A user can open docs/launch-post.md and see, in the locked D-POST-ORDER order, a block-quoted personal-sentence placeholder, then the value prop, then a 30-second pitch, then a 60-second install, then expected first-week outcome, then a feedback line."
    - "A user can copy the install block verbatim from the launch post and run it on a fresh machine — the six lines match README.md lines 124-129 + a sixth `self-wiki doctor` line."
    - "A user can read the post in Slack's 'before Show more' window and see the value prop + first pitch bullet without expanding (~1000-1500 chars total)."
    - "A user editing the post finds exactly two scoped placeholders to touch before sending: the optional personal sentence block-quote and the Slack channel parenthetical."
    - "A reader who follows the feedback line lands on either `#self-wiki-feedback (TODO: confirm channel name)` (questions) or https://github.com/liferay-appsec/liferay-self-wiki/issues (bugs / feature requests)."
    - "Nothing in the post mentions npm registry publish, public github.com, v2.0 features, or emojis / shortcodes — the distribution model is stated honestly (clone + `npm install -g .` from `liferay-appsec/liferay-self-wiki`)."
  artifacts:
    - path: "docs/launch-post.md"
      provides: "Slack announcement draft, GitHub-flavored markdown, paste-edit-ready."
      min_lines: 20
      contains: "self-wiki doctor"
  key_links:
    - from: "docs/launch-post.md (60-second install section)"
      to: "README.md `## Install` section"
      via: "prose link to the README"
      pattern: "README"
    - from: "docs/launch-post.md (expected first-week outcome section)"
      to: "docs/examples/{daily-log,weekly-report,monthly-report,self-review}.md"
      via: "markdown links"
      pattern: "docs/examples/"
    - from: "docs/launch-post.md (feedback line)"
      to: "Slack channel placeholder + GitHub Issues URL"
      via: "both surfaces named with intent (D-FEEDBACK-BOTH-SURFACES)"
      pattern: "#self-wiki-feedback \\(TODO: confirm channel name\\)"
---

<objective>
Author `docs/launch-post.md` — a new file containing the Slack
announcement draft for self-wiki v1.0. The post is GitHub-flavored
markdown (D-POST-FORMAT-GFM), ~1000–1500 chars / ~20–30 lines
(D-POST-LENGTH), with sections in the locked D-POST-ORDER. A user
will paste it into Slack, edit 2–3 placeholders (the optional
personal sentence block-quote and the Slack channel parenthetical),
and send it without further rewriting.

Purpose: ship the launch artifact LAUNCH-01 requires. The post is
the on-arrival hook for every Liferay engineer who sees the
announcement; it points readers at the README install flow + the
`docs/examples/` shape preview + the two feedback surfaces.

Output: one new file at `docs/launch-post.md`. No `src/` changes,
no test changes, no README changes (LAUNCH-02 handles README in
plan 07-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-launch-kit/07-CONTEXT.md
@README.md
@docs/examples/daily-log.md
@docs/examples/weekly-report.md
@docs/examples/monthly-report.md
@docs/examples/self-review.md

<interfaces>
<!--
The launch post mirrors the README's install block byte-for-byte
plus one extra line. The block below is the verbatim shape from
README.md lines 123-129 — the executor copies it then adds the
`self-wiki doctor` line as the sixth command (D-INSTALL-FENCED-BLOCK).
-->

From README.md lines 123-129 (the canonical install block — copy
verbatim, then add `self-wiki doctor` as line six):

```sh
git clone https://github.com/liferay-appsec/liferay-self-wiki.git self-wiki
cd self-wiki
npm install
npm install -g .
self-wiki init /path/to/your/vault
```

The launch post's install block adds `self-wiki doctor` as a sixth
line. D-INSTALL-FENCED-BLOCK locks this exact shape; the doctor line
is the post-install handshake Phase 06 promised.

From `.planning/phases/07-launch-kit/07-CONTEXT.md` <specifics>
section — the verbatim feedback strings that MUST appear in this
file (and also in `README.md` via plan 07-02):

- Slack channel placeholder: `#self-wiki-feedback (TODO: confirm channel name)`
- GitHub Issues URL: `https://github.com/liferay-appsec/liferay-self-wiki/issues`
- Distribution-honest sentence shape: "Internal Liferay tool. Distribution stays clone + `npm install -g .` from the `liferay-appsec/` GitHub org — no npm registry publish, no public github.com."

From `docs/examples/` — the four scrubbed-artifact paths the
first-week outcome section links to (D-FIRSTWEEK-LINK-EXAMPLES):

- `docs/examples/daily-log.md`
- `docs/examples/weekly-report.md`
- `docs/examples/monthly-report.md`
- `docs/examples/self-review.md`

These four files already exist (Phase 05). Plan 07-01 does NOT
modify them; it only links to them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/launch-post.md in the locked section order</name>
  <files>docs/launch-post.md</files>
  <read_first>
    - `.planning/phases/07-launch-kit/07-CONTEXT.md` (full file — locked decisions D-POST-ORDER, D-POST-FORMAT-GFM, D-POST-LENGTH, D-POST-PERSONAL-SENTENCE, D-PITCH-OUTCOME-LED, D-PITCH-LEAD-WITH-SELF-REVIEW, D-INSTALL-FENCED-BLOCK, D-INSTALL-DISTRIBUTION-HONEST, D-FIRSTWEEK-CONCRETE-ARTIFACTS, D-FIRSTWEEK-LINK-EXAMPLES, D-FEEDBACK-BOTH-SURFACES, D-FEEDBACK-PLACEHOLDER-VERBATIM, D-NO-V2-MENTIONS, D-NO-EMOJIS — these are non-negotiable)
    - `README.md` lines 117–143 (the `## Install` section — the install block at lines 123–129 is the byte-source for this post's 60-second install fenced block)
    - `README.md` lines 7–89 (the `## What you get` section — the planner mines this for outcome vocabulary like "themed weekly report (decisions, lessons, carry-over)"; do NOT inline snippets from here — the launch post points at the README, it does not duplicate it)
    - `docs/examples/daily-log.md`, `docs/examples/weekly-report.md`, `docs/examples/monthly-report.md`, `docs/examples/self-review.md` (verify the four paths exist and are spelled exactly — the first-week-outcome section links to all four)
    - `.planning/PROJECT.md` (the distribution model + Liferay context the post's voice should match)
  </read_first>
  <action>
    Create a new file at `docs/launch-post.md`. Per D-POST-FORMAT-GFM,
    write **GitHub-flavored markdown**: `##` headers, `**bold**`, `-`
    bullets, fenced code blocks with `sh` language tag. NO emojis,
    NO shortcodes (`:rocket:`, `:tada:`, etc.) per D-NO-EMOJIS.
    NO exclamation marks (carry-forward from Phase 05's tone). Voice:
    peer-to-peer, Liferay-engineer-to-Liferay-engineer — dry, no
    marketing buzzwords.

    Sections appear in this **exact** order per D-POST-ORDER:

    **1. Title (`# self-wiki`) + optional personal-sentence
    block-quote placeholder.** The placeholder lives between the
    title and the value prop. Use this exact shape per
    D-POST-PERSONAL-SENTENCE (the `_…_` italics and the explicit
    "or delete the whole quote" are load-bearing — the user must
    not miss the placeholder):
    ```markdown
    > _Optional: replace this line with one sentence on why you tried
    > self-wiki and what stuck — or delete the whole quote before
    > sending._
    ```

    **2. `## What it is` (one-sentence value prop).** One sentence
    only. Match the README's voice. Reasonable shape: "A
    self-writing personal wiki for engineers — every `claude`
    session inside a repo leaves behind a daily log, a per-ticket
    history, and a weekly report, with zero per-session commands
    from you." Planner discretion on the exact wording within
    D-POST-LENGTH; the constraint is one sentence + outcome-shaped.

    **3. `## What you get in 30 seconds` (3–4 outcome-led
    bullets).** Per D-PITCH-OUTCOME-LED, bullets describe what the
    engineer ends up with, not what the tool does. Per
    D-PITCH-LEAD-WITH-SELF-REVIEW, **bullet 1 leads with the
    self-review outcome** (Liferay review cycle, paste-ready draft,
    values-tagged). Indicative bullet set (planner refines wording;
    structure locked here):
      - Bullet 1 (self-review, cycle close): a paste-ready
        Liferay-form self-review draft, tagged by the five values,
        with monthly + weekly sources cited.
      - Bullet 2 (weekly): every week, a themed weekly report
        (decisions, lessons, carry-over), built from your daily
        logs.
      - Bullet 3 (daily): every day, terse decision/outcome notes
        from Claude land in `Daily/<date>.md` — no per-session
        command from you.
      - Bullet 4 (ticket history): per-ticket history that grows
        across sessions in `Tickets/LPD-xxxxx.md` (use `LPD-` per
        Phase 05 D-EXAMPLES-PREFIX — the launch post is about real
        Liferay work, not the demo storyline).

    **4. `## 60-second install` (fenced `sh` block + one prose
    line linking to the README + the honest-distribution sentence).**
    Per D-INSTALL-FENCED-BLOCK, the fenced block is **six lines** in
    this exact order — five lines from README.md lines 124–128
    verbatim, plus `self-wiki doctor` as the sixth line:
    ```sh
    git clone https://github.com/liferay-appsec/liferay-self-wiki.git self-wiki
    cd self-wiki
    npm install
    npm install -g .
    self-wiki init /path/to/your/vault
    self-wiki doctor
    ```
    Below the fenced block, one prose sentence linking to the
    README install section for the full flow ("See the [README
    install section](../README.md#install) for the prerequisites
    and what `init` does." or similar — planner discretion on
    exact wording).

    Then the **honest-distribution sentence**, per
    D-INSTALL-DISTRIBUTION-HONEST. Use this shape (planner may
    polish but the substance is locked by success criterion 4):
    > Internal Liferay tool. Distribution stays clone +
    > `npm install -g .` from the `liferay-appsec/` GitHub org —
    > no npm registry publish, no public github.com.

    **5. `## Expected first-week outcome` (concrete-artifact list +
    one-line examples pointer).** Per D-FIRSTWEEK-CONCRETE-ARTIFACTS,
    this is a concrete-artifact list — what files exist in the
    vault after five working days. Recommended four bullets (keep
    it tight per Claude's Discretion guidance):
      - ~5 files in `Daily/` — one per working day, with whatever
        `self-wiki note` lines Claude dropped while you worked.
      - A handful of `Tickets/LPD-xxxxx.md` files growing across
        the week for the tickets you touched.
      - A `Components/<slug>.md` page or two if you crossed the
        same code surface in multiple sessions.
      - One weekly report at `Reports/YYYY-Www.md` produced by
        `self-wiki report --week` (one command, ~30 seconds).

    Then a one-line pointer per D-FIRSTWEEK-LINK-EXAMPLES:
    > Preview the shapes: [daily log](../docs/examples/daily-log.md),
    > [weekly report](../docs/examples/weekly-report.md),
    > [monthly report](../docs/examples/monthly-report.md),
    > [self-review draft](../docs/examples/self-review.md).

    NOTE on relative link paths: `docs/launch-post.md` lives at
    `docs/launch-post.md`, so links to siblings in `docs/examples/`
    are written as `examples/<file>.md` (NOT `docs/examples/…`
    and NOT `../docs/examples/…`). Resolve relative paths correctly
    when writing the file; the README's links use `docs/examples/…`
    because the README is at the repo root.

    **6. `## Feedback` (the feedback line — both surfaces named
    with intent).** Per D-FEEDBACK-BOTH-SURFACES + D-FEEDBACK-
    PLACEHOLDER-VERBATIM. The Slack channel placeholder string
    is **literal** — `#self-wiki-feedback (TODO: confirm channel
    name)` — and appears verbatim. Two clauses, intent-led:
    > Quick chatter / questions: `#self-wiki-feedback (TODO:
    > confirm channel name)` on Liferay Slack.
    >
    > Bugs / feature requests: https://github.com/liferay-appsec/liferay-self-wiki/issues

    **Length budget per D-POST-LENGTH:** target 1000–1500 chars
    total file size; ~20–30 lines including bullets. Slack will
    collapse beyond ~700 chars under "Show more"; the value prop +
    bullet 1 should sit above that threshold. After writing,
    check `wc -c docs/launch-post.md` — if it's >1500, tighten
    prose; if <1000, the post probably dropped a required section.

    **Forbidden tokens** (success criterion 4 + D-NO-V2-MENTIONS +
    D-NO-EMOJIS — do NOT use any of these):
      - `npm publish`, `npmjs.com`, `registry`
      - `v2.0`, `v2`, future-feature names (`TREND-`, `TOOL-`,
        "year-over-year", "preview mid-cycle")
      - "open source", "public release"
      - github.com URLs outside `liferay-appsec/` (the launch
        post never references public github.com)
      - `:rocket:`, `:tada:`, `:wave:`, `:party:`, or any other
        emoji shortcodes
      - Unicode emoji glyphs (e.g. 🚀 ✨ 🎉)
      - Exclamation marks
  </action>
  <verify>
    <automated>
      bash -c 'F=docs/launch-post.md; \
        test -f "$F" || { echo "FAIL: $F missing"; exit 1; }; \
        wc -c "$F" | awk "{ if (\$1 < 1000 || \$1 > 1500) { print \"FAIL: length\", \$1, \"outside 1000-1500\"; exit 1 } else { print \"OK: length\", \$1 } }" && \
        grep -c "^## " "$F" | awk "{ if (\$1 < 4) { print \"FAIL: only\", \$1, \"H2 sections, need >= 4\"; exit 1 } else { print \"OK: H2 count\", \$1 } }" && \
        grep -Fxq "self-wiki doctor" "$F" && echo "OK: doctor line present in install block" && \
        grep -cF "#self-wiki-feedback (TODO: confirm channel name)" "$F" | awk "{ if (\$1 != 1) { print \"FAIL: Slack placeholder count\", \$1, \"!= 1\"; exit 1 } else { print \"OK: Slack placeholder present exactly once\" } }" && \
        grep -cF "https://github.com/liferay-appsec/liferay-self-wiki/issues" "$F" | awk "{ if (\$1 != 1) { print \"FAIL: GitHub Issues URL count\", \$1, \"!= 1\"; exit 1 } else { print \"OK: GitHub Issues URL present exactly once\" } }" && \
        grep -F "liferay-appsec/" "$F" >/dev/null && echo "OK: liferay-appsec org named" && \
        ! grep -Ei "npm publish|npmjs\.com|registry" "$F" && echo "OK: no npm-publish references" && \
        ! grep -E "v2\.0|v2 " "$F" && echo "OK: no v2 references" && \
        ! grep -E ":rocket:|:tada:|:wave:|:party:|:sparkles:|:fire:" "$F" && echo "OK: no emoji shortcodes" && \
        ! grep -E "(🚀|🎉|✨|🔥|👋|🎊)" "$F" && echo "OK: no emoji glyphs" && \
        ! grep -F "!" "$F" && echo "OK: no exclamation marks" && \
        echo "ALL CHECKS PASSED"'
    </automated>
  </verify>
  <done>
    `docs/launch-post.md` exists, is 1000–1500 chars, contains
    >=4 H2 sections in D-POST-ORDER order (`## What it is`,
    `## What you get in 30 seconds`, `## 60-second install`,
    `## Expected first-week outcome`, `## Feedback`), the install
    block is the README's five lines + a sixth `self-wiki doctor`
    line, the Slack placeholder appears once verbatim
    (`#self-wiki-feedback (TODO: confirm channel name)`), the
    GitHub Issues URL appears once
    (`https://github.com/liferay-appsec/liferay-self-wiki/issues`),
    the four `docs/examples/` files are linked in the first-week
    section, the honest-distribution sentence names
    `liferay-appsec/` and disclaims npm publish + public
    github.com, and the file is free of emojis / shortcodes /
    exclamation marks / v2 references / npm-publish references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Non-regression smoke — tests + CLI still pass</name>
  <files></files>
  <read_first>
    - The terminal output from Task 1 (file exists + content checks passed).
  </read_first>
  <action>
    Phase 07 is documentation-only — Task 1 created exactly one new
    file at `docs/launch-post.md` and modified nothing else.
    Confirm the test suite and CLI surface still work:

    1. Run `npm test`. The 257-test suite from Phase 06 must stay
       green.
    2. Run `self-wiki --help`. The command must print its usage
       block (Commander output) without error and exit zero. If
       `self-wiki` is not on PATH, run `node src/cli.js --help`
       instead — same expected output shape.

    No code was touched; this is a defensive "did anything regress"
    pass. If either step fails, treat it as a real regression and
    investigate before declaring the plan done. Do NOT modify the
    test suite to make a failure pass.
  </action>
  <verify>
    <automated>
      bash -c 'npm test 2>&1 | tail -20 | grep -E "tests [0-9]+|pass [0-9]+|# tests [0-9]+" && \
        (self-wiki --help 2>/dev/null || node src/cli.js --help) | head -1 | grep -F "Usage" && \
        echo "OK: tests pass + CLI prints help"'
    </automated>
  </verify>
  <done>
    `npm test` passes (257/257 from Phase 06 stays green — no
    new tests added, no existing tests modified) AND
    `self-wiki --help` (or `node src/cli.js --help`) prints the
    Commander usage block. Confirms Phase 07 doc-only writes did
    not regress the runtime surface.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → docs/launch-post.md | This plan writes a new doc file. No untrusted input crosses any boundary at runtime — the file is plain markdown the user later pastes into Slack. Slack itself sanitizes pasted content. |

## STRIDE Threat Register

Phase 07 plan 01 is documentation-only: no new code paths, no new
commands, no new flags, no new permissions, no dependency changes.
The threat surface is negligible.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `docs/launch-post.md` references the internal GitHub repo URL `https://github.com/liferay-appsec/liferay-self-wiki/issues` | accept | The URL is already public in `CONTRIBUTING.md` (Phase 04 D-ISSUE-DEST) and `README.md`; it is not a secret. The launch post is also paste-edited by the user before posting to internal Slack, so distribution stays inside Liferay. |
| T-07-02 | Spoofing | Slack channel placeholder `#self-wiki-feedback (TODO: confirm channel name)` — could a reader misread the placeholder as a live channel? | mitigate | The literal parenthetical `(TODO: confirm channel name)` is unambiguous per D-FEEDBACK-PLACEHOLDER-VERBATIM. Success criterion 3 explicitly forbids placeholders that can be mistaken for live content. Verified by grep in the acceptance checks (Task 1). |
| T-07-03 | Tampering | Markdown rendering in Slack — could Slack's renderer cause confusion (e.g. links not rendering, formatting breaking)? | accept | The post is GitHub-flavored markdown (D-POST-FORMAT-GFM); the user paste-edits 2–3 placeholders before sending, which is also their chance to spot any rendering glitch in Slack's preview. Defer to user judgment. |
</threat_model>

<verification>
- File `docs/launch-post.md` exists.
- File length is 1000–1500 chars (D-POST-LENGTH).
- File contains >=4 `## ` headers in D-POST-ORDER order.
- File contains `self-wiki doctor` (D-INSTALL-FENCED-BLOCK sixth line).
- File contains `#self-wiki-feedback (TODO: confirm channel name)` exactly once (D-FEEDBACK-PLACEHOLDER-VERBATIM).
- File contains `https://github.com/liferay-appsec/liferay-self-wiki/issues` exactly once (D-FEEDBACK-BOTH-SURFACES).
- File links to all four `docs/examples/{daily-log,weekly-report,monthly-report,self-review}.md` paths (D-FIRSTWEEK-LINK-EXAMPLES).
- File contains the `liferay-appsec/` org name (D-INSTALL-DISTRIBUTION-HONEST).
- File contains no npm-publish / npmjs.com / registry references (success criterion 4, D-NO-V2-MENTIONS).
- File contains no v2 / v2.0 references (D-NO-V2-MENTIONS).
- File contains no emoji shortcodes (`:rocket:` etc) or emoji glyphs (D-NO-EMOJIS).
- File contains no exclamation marks (Phase 05 voice carry-forward).
- `npm test` still passes (257/257 from Phase 06).
- `self-wiki --help` still prints.
</verification>

<success_criteria>
LAUNCH-01 satisfied:
- A user can open `docs/launch-post.md`, paste it into Slack, edit
  the optional personal-sentence block-quote and the Slack channel
  parenthetical, and send it without further rewriting (success
  criterion 1).
- The file contains the five required sections in the locked
  order (success criterion 2).
- Nothing in the file references npm registry publish, public
  github.com, or v2.0 features; the distribution model is stated
  honestly (success criterion 4).
- The 257-test suite from Phase 06 stays green and
  `self-wiki --help` still prints (non-regression).
</success_criteria>

<output>
After completion, create
`.planning/phases/07-launch-kit/07-01-SUMMARY.md` per the
get-shit-done summary template.
</output>
