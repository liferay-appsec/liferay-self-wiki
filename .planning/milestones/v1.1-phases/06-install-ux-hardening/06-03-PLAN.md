---
phase: 06-install-ux-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - README.md
autonomous: true
requirements:
  - INST-03
must_haves:
  goal: >
    README.md contains a `## Troubleshooting` section between `## Upgrading`
    and `## License` that opens with a one-line pointer to `self-wiki doctor`
    and contains a three-row pipe table mapping the three REQ-INST-03
    symptoms (sessions not opening, notes not landing, approval prompts) to
    the doctor check label that diagnoses each and the `self-wiki init
    --X-only` fix command — with NO inlined doctor sample output.
  truths:
    - "`README.md` contains the line `## Troubleshooting` exactly once."
    - "The `## Troubleshooting` section appears AFTER `## Upgrading` and BEFORE `## License` in `README.md` (line-order verifiable)."
    - "The opening sentence of the Troubleshooting section is `Run \\`self-wiki doctor\\` to diagnose your install. Each ✓/✗ is followed by a one-line remediation. Below: common symptoms keyed to specific doctor checks.` — verbatim per D-NO-SAMPLE."
    - "The Troubleshooting table has exactly THREE data rows (plus header + separator) — one per REQ-INST-03 symptom, NOT one per check (per D-SYMPTOM-ROWS)."
    - "Row 1 symptom: `Sessions not opening in new repos`; check column references `hooks merged in settings.json` ✗ AND the `i hooks:` drift line (mirroring Row 3's symmetric ✗-or-drift shape — both rows surface the same Tier-1/Tier-2 dichotomy from src/commands/doctor.js); fix column: `run \\`self-wiki init --hooks-only\\``."
    - "Row 2 symptom: `No notes captured in the daily log` (matches REQ-INST-03's verbatim 'no notes captured' phrasing); check column references both `vault config present` and `vault path exists on disk`; fix column: `run \\`self-wiki init <vault>\\``."
    - "Row 3 symptom: `Approval prompts during \\`claude\\` turns`; check column references `permissions merged in settings.json` AND the `i permissions:` drift line; fix column: `run \\`self-wiki init --permissions-only\\``."
    - "The Troubleshooting section contains NO inlined doctor sample output (no fenced code block showing a happy-path or failure-mode rendering of `self-wiki doctor` — per D-NO-SAMPLE)."
    - "The Troubleshooting section contains NO emojis (per D-TROUBLESHOOTING-NO-EMOJI). The Unicode `✓`, `✗`, `→`, `i` symbols are NOT emojis — they are geometric symbols matching the doctor output convention, and `✗` is used in the table cells where it quotes a failing check."
    - "Every doctor check label quoted in the Troubleshooting table appears verbatim in `src/commands/doctor.js` (grep-verifiable cross-source identity). Both Tier-2 drift tokens (`i hooks:`, `i permissions:`) likewise appear verbatim in doctor.js."
  artifacts:
    - path: README.md
      provides: "Public-facing troubleshooting guide for fresh Liferay devs; opens with pointer to `self-wiki doctor`, three-row symptom-to-check-to-fix table."
      contains: "## Troubleshooting"
  key_links:
    - from: README.md (Troubleshooting table column 2)
      to: src/commands/doctor.js (the seven load-bearing English labels + two Tier-2 drift tokens)
      via: "Column 2 of the Troubleshooting table quotes doctor check labels VERBATIM. The seven labels — `Node ≥ 20`, `claude CLI on PATH`, `vault config present`, `vault path exists on disk`, `hooks merged in settings.json`, `permissions merged in settings.json`, `wiki skill installed` — are the load-bearing string contract. Rows 1 and 3 also reference the Tier-2 drift tokens `i hooks:` and `i permissions:` verbatim."
      pattern: "hooks merged in settings.json"
    - from: README.md (Troubleshooting table column 3)
      to: src/commands/init.js (the three narrow-fix flags from Plan 06-02)
      via: "Fix column references the three narrow-fix flags (`--hooks-only`, `--permissions-only`) and the full `init <vault>` command. These commands exist after Plan 06-02 ships."
      pattern: "self-wiki init --hooks-only"
---

<objective>
Ship the README Troubleshooting section (INST-03). Insert a new
`## Troubleshooting` heading between `## Upgrading` and `## License` in
`README.md`, with a one-line opener pointing the reader at `self-wiki
doctor` and a three-row pipe table mapping the three REQ-INST-03 symptoms
to the specific doctor check label that diagnoses each and the `self-wiki
init --X-only` command that fixes it. No inlined doctor sample output (per
D-NO-SAMPLE). No emojis (per D-TROUBLESHOOTING-NO-EMOJI).

Purpose: A user who hits a confusing symptom (session not opening, notes
not landing, permission prompts during turns) needs a one-line map from
symptom → doctor check → fix command. The table does NOT replace doctor's
own output — it points at it. This completes the trust loop a fresh Liferay
dev needs to recover from a misconfigured install without guessing.

Output: `README.md` extended with one new section (~8 lines: heading +
opening sentence + blank line + 4 table lines + blank line). No other
README content touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-install-ux-hardening/06-CONTEXT.md
@.planning/phases/06-install-ux-hardening/06-PATTERNS.md
@.planning/phases/05-public-grade-documentation/05-CONTEXT.md
@CLAUDE.md

@README.md
@src/commands/doctor.js
</context>

<interfaces>
<!-- The seven load-bearing doctor check labels from Plan 06-01. -->
<!-- Column 2 of the Troubleshooting table must quote these VERBATIM. -->

From `src/commands/doctor.js` (Plan 06-01 output):

| # | Section in doctor output | Verbatim label |
|---|--------------------------|----------------|
| 1 | Runtime | `Node ≥ 20` |
| 2 | Runtime | `claude CLI on PATH` |
| 3 | Vault | `vault config present` |
| 4 | Vault | `vault path exists on disk` |
| 5 | Claude Code wiring | `hooks merged in settings.json` |
| 6 | Claude Code wiring | `permissions merged in settings.json` |
| 7 | Claude Code wiring | `wiki skill installed` |

Plus the Tier-2 drift line tokens (used in row 1's AND row 3's check
columns for symmetric ✗-or-drift coverage):
- `i hooks: ...` (hooks drift — primary diagnostic for "sessions not
  opening" after a self-wiki upgrade, since Tier-1 may still pass while
  individual commands have drifted from the template)
- `i permissions: ...` (permissions drift — surfaces missing entries when
  Tier-1 still passes with at least one match)

The fix commands (from Plan 06-02's narrow-fix flags):
- `self-wiki init --hooks-only` — fixes hooks-related ✗ or drift.
- `self-wiki init --permissions-only` — fixes permissions ✗ or drift.
- `self-wiki init --skill-only` — fixes wiki-skill ✗.
- `self-wiki init <vault>` — fixes vault config or vault path ✗.

Current README structure (verified from the project's README.md):

```
... earlier sections ...
## Configuration             (line ~234)
## Parallel Claude Code sessions  (line ~244)
## Upgrading                 (line ~250)
[Upgrading content, lines 252-264]
## License                   (line ~266)
[License content, lines 268-269]
```

The Upgrading section ends at line ~264 with a paragraph beginning with
"Re-running `init` after upgrading is a no-op when nothing changed..." and
`## License` follows immediately on line ~266 after a single blank line.

The new section inserts between them: after the Upgrading section's last
content line, with a blank line above and below the new `## Troubleshooting`
heading, and the existing `## License` heading remains in place after.

Phase 07 will append a `## Support / Feedback` section AFTER `## License`
— the Troubleshooting section sits between Upgrading and License regardless
of what Phase 07 adds.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Insert the Troubleshooting section between Upgrading and License in README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (read fully — especially lines 250-269 for the Upgrading and License sections, plus lines 145-153 for the existing pipe-table format convention)
    - src/commands/doctor.js (the post-Plan-06-01 file — confirms the seven verbatim label strings the table will quote, plus the `i hooks:` and `i permissions:` drift tokens)
    - .planning/REQUIREMENTS.md (REQ-INST-03 verbatim — Row 2 symptom text matches the "no notes captured" phrasing)
    - .planning/phases/06-install-ux-hardening/06-CONTEXT.md (D-PLACEMENT, D-SYMPTOM-ROWS, D-NO-SAMPLE, D-TROUBLESHOOTING-NO-EMOJI)
    - .planning/phases/06-install-ux-hardening/06-PATTERNS.md (section "README.md" — Analog A opener, Analog B pipe-table format, three-row table verbatim)
    - .planning/phases/05-public-grade-documentation/05-CONTEXT.md (D-NO-P07-STUB carry-forward, no-emojis-in-markdown convention, `**Example output:**` literal label convention)
    - CLAUDE.md (no Liferay,Inc. branding anywhere — moot for this section, but a guardrail; no-obsidian-cli rule — also moot)
  </read_first>
  <action>
Open `README.md` and locate the exact insertion point: the boundary between
the end of the Upgrading section (the paragraph ending "Daily logs, topic
pages, and weekly reports are never touched.") and the `## License`
heading.

Insert the following EXACT block between those two existing sections,
preserving one blank line on each side of the new heading so the final
structure reads:

```
... (existing Upgrading section ends with this paragraph) ...
Re-running `init` after upgrading is a no-op when nothing changed, but it picks up new `Bash(self-wiki …)` permission rules (e.g. `close-orphans`) and refreshes the skill primer when the closing-summary phrase list grows. Daily logs, topic pages, and weekly reports are never touched.

## Troubleshooting

Run `self-wiki doctor` to diagnose your install. Each ✓/✗ is followed by a one-line remediation. Below: common symptoms keyed to specific doctor checks.

| Symptom | Doctor check | Fix |
| --- | --- | --- |
| Sessions not opening in new repos | `hooks merged in settings.json` ✗ or `i hooks:` drift line | run `self-wiki init --hooks-only` |
| No notes captured in the daily log | `vault config present` ✗ or `vault path exists on disk` ✗ | run `self-wiki init <vault>` |
| Approval prompts during `claude` turns | `permissions merged in settings.json` ✗ or `i permissions:` drift line | run `self-wiki init --permissions-only` |

## License

Apache 2.0. See [LICENSE](LICENSE) for the license text and [NOTICE](NOTICE) for copyright attribution.
```

Verbatim contract notes:

1. The heading is exactly `## Troubleshooting` (capital T, no trailing
   punctuation, level-2 ATX heading). NOT `### Troubleshooting`,
   NOT `## Troubleshooting:`, NOT `## Troubleshooting (FAQ)`.

2. The opening sentence is exactly the D-NO-SAMPLE-locked string:
   `Run \`self-wiki doctor\` to diagnose your install. Each ✓/✗ is followed by a one-line remediation. Below: common symptoms keyed to specific doctor checks.`
   The backticks around `self-wiki doctor` and the literal `✓`/`✗`
   characters (NOT emojis) are part of the sentence. Do not reword.

3. The table has EXACTLY 5 lines:
   - Header: `| Symptom | Doctor check | Fix |`
   - Separator: `| --- | --- | --- |` (three dashes per cell — NOT padded
     with extra dashes; the most concise form is the cleanest here and is
     consistent with markdown rendering)
   - Row 1 (sessions): `| Sessions not opening in new repos | \`hooks merged in settings.json\` ✗ or \`i hooks:\` drift line | run \`self-wiki init --hooks-only\` |`
   - Row 2 (notes): `| No notes captured in the daily log | \`vault config present\` ✗ or \`vault path exists on disk\` ✗ | run \`self-wiki init <vault>\` |`
   - Row 3 (permissions): `| Approval prompts during \`claude\` turns | \`permissions merged in settings.json\` ✗ or \`i permissions:\` drift line | run \`self-wiki init --permissions-only\` |`

   Row 1 and Row 3 are structurally parallel — both follow the
   `\`Tier-1-label\` ✗ or \`i <event>:\` drift line` shape — because hook
   drift is the primary diagnostic for "sessions not opening" after a
   self-wiki upgrade. A user whose Tier-1 hooks check still passes but whose
   commands have drifted from the template would otherwise see no signal
   in the Troubleshooting table; the `i hooks:` drift line closes that gap.

   Row 2 symptom matches REQ-INST-03's verbatim "no notes captured" phrasing
   (the requirement says: "When no notes are captured in the daily log, the
   user is pointed at a vault-related check"). The closest matching English
   for the table cell is `No notes captured in the daily log`.

4. Backticks are used around:
   - The command `self-wiki doctor` in the opener sentence.
   - Each doctor check label in column 2 (`hooks merged in settings.json`,
     `vault config present`, `vault path exists on disk`, `permissions
     merged in settings.json`).
   - The drift tokens `i hooks:` (row 1) and `i permissions:` (row 3) in
     column 2.
   - The command `claude` in row 3 column 1.
   - Each fix command in column 3.

5. The `✗` character (U+2717) is used VERBATIM in column 2 cells where the
   table refers to a failing check. Do NOT replace with `(fail)` or `x` or
   any emoji. It matches the exact character doctor.js prints.

6. The `<vault>` placeholder in row 2's fix is a literal angle-bracketed
   placeholder, not an HTML tag. It appears as `<vault>` in the rendered
   table.

7. NO fenced code block (` ```sh ` or ` ```text `) is added in this
   section. Per D-NO-SAMPLE, the README points at `self-wiki doctor` and
   describes its output shape (in prose), but does NOT inline a sample
   rendering — that would create a sync risk with doctor's actual output.

8. NO emojis anywhere in the section. `✓`, `✗`, `→`, `i` (Unicode geometric
   symbols) are explicitly NOT emojis per D-TROUBLESHOOTING-NO-EMOJI and
   the project's no-emojis-in-markdown carry-forward from Phase 04/05.

9. The Upgrading section content remains UNCHANGED. Do not edit any text
   within `## Upgrading`. Only insert the new section after it.

10. The License section content remains UNCHANGED. Do not edit
    `## License`, [LICENSE], or [NOTICE] references.

11. No edits to ANY other part of README.md. Phase 05 closed the README
    layout; Phase 06 only inserts this one section. Phase 07 will later
    append `## Support / Feedback` AFTER `## License`.

12. NO mention of Liferay, Inc., the user's name, or the user's vault
    contents in the new section. The Troubleshooting section is generic.

Sanity check before committing: open `README.md` and verify the table
renders correctly (visually) by counting pipes in each row: header has 4
pipes (or 3 + leading/trailing), each row has the same count. Markdown
tables tolerate trailing pipe variations, but the form above uses leading
AND trailing pipes which is the safest portable form.

Also verify line order by greps (see acceptance criteria) — the order
must be: `## Upgrading` < `## Troubleshooting` < `## License`.
  </action>
  <verify>
    <automated>cd /home/me/dev/projects/liferay-self-wiki && grep -c "^## Troubleshooting$" README.md</automated>
  </verify>
  <acceptance_criteria>
    - Heading exists exactly once: `grep -c "^## Troubleshooting$" README.md` returns 1.
    - Section ordering — Upgrading < Troubleshooting < License (verify line numbers numerically):
      - `L_up = grep -n "^## Upgrading$" README.md | cut -d: -f1`
      - `L_ts = grep -n "^## Troubleshooting$" README.md | cut -d: -f1`
      - `L_lic = grep -n "^## License$" README.md | cut -d: -f1`
      - L_up < L_ts < L_lic numerically (run `awk -F: 'NR==1{u=$1} NR==2{t=$1} NR==3{l=$1} END{exit !(u<t && t<l)}' <(grep -n "^## \\(Upgrading\\|Troubleshooting\\|License\\)$" README.md)`; expect exit 0).
    - Opening sentence is verbatim:
      - `grep -F "Run \`self-wiki doctor\` to diagnose your install. Each ✓/✗ is followed by a one-line remediation. Below: common symptoms keyed to specific doctor checks." README.md` returns 1 hit.
    - Three table rows present, each with the symptom + check + fix verbatim:
      - `grep -F "| Sessions not opening in new repos | \`hooks merged in settings.json\` ✗ or \`i hooks:\` drift line | run \`self-wiki init --hooks-only\` |" README.md` returns 1.
      - `grep -F "| No notes captured in the daily log | \`vault config present\` ✗ or \`vault path exists on disk\` ✗ | run \`self-wiki init <vault>\` |" README.md` returns 1.
      - `grep -F "| Approval prompts during \`claude\` turns | \`permissions merged in settings.json\` ✗ or \`i permissions:\` drift line | run \`self-wiki init --permissions-only\` |" README.md` returns 1.
    - Row 1 references both the Tier-1 ✗ label AND the Tier-2 `i hooks:` drift token (mirrors Row 3's symmetric shape):
      - `grep -F "\`i hooks:\` drift line" README.md` returns ≥ 1.
      - `grep -F "\`i hooks:\`" README.md` returns ≥ 1.
    - Row 2 symptom text matches REQ-INST-03 verbatim "no notes captured" phrasing:
      - `grep -F "No notes captured in the daily log" README.md` returns ≥ 1.
    - Table header + separator present:
      - `grep -F "| Symptom | Doctor check | Fix |" README.md` returns 1.
      - `grep -F "| --- | --- | --- |" README.md` returns ≥ 1 (this exact separator might appear in other tables in the future; right now it returns ≥ 1).
    - Cross-source label identity — every doctor check label quoted in the table appears verbatim in BOTH README.md AND src/commands/doctor.js:
      - `grep -F "hooks merged in settings.json" README.md` returns ≥ 1 AND `grep -F "hooks merged in settings.json" src/commands/doctor.js` returns ≥ 1.
      - `grep -F "vault config present" README.md` returns ≥ 1 AND `grep -F "vault config present" src/commands/doctor.js` returns ≥ 1.
      - `grep -F "vault path exists on disk" README.md` returns ≥ 1 AND `grep -F "vault path exists on disk" src/commands/doctor.js` returns ≥ 1.
      - `grep -F "permissions merged in settings.json" README.md` returns ≥ 1 AND `grep -F "permissions merged in settings.json" src/commands/doctor.js` returns ≥ 1.
    - Cross-source identity for the two Tier-2 drift tokens (both rows that reference them quote doctor.js verbatim):
      - `grep -F "i hooks:" README.md` returns ≥ 1 AND `grep -F "i hooks:" src/commands/doctor.js` returns ≥ 1.
      - `grep -F "i permissions:" README.md` returns ≥ 1 AND `grep -F "i permissions:" src/commands/doctor.js` returns ≥ 1.
    - No inlined doctor sample (D-NO-SAMPLE):
      - The Troubleshooting section does NOT contain a fenced code block. Run:
        `awk '/^## Troubleshooting$/,/^## License$/' README.md | grep -c "^\`\`\`"` returns 0.
    - No emojis introduced (D-TROUBLESHOOTING-NO-EMOJI):
      - The Troubleshooting section contains no emoji-block characters. The Unicode `✓`, `✗`, `→`, `i` are not emojis. Run:
        `awk '/^## Troubleshooting$/,/^## License$/' README.md | grep -cP "[\x{1F300}-\x{1F9FF}]"` returns 0.
        (PCRE Unicode range covers the main emoji blocks — Miscellaneous Symbols and Pictographs through Supplemental Symbols and Pictographs. Available everywhere grep is built with PCRE support; no python3 dependency.)
    - Three-row table — exactly three pipe-table data rows in the section (not 7-rows-per-check):
      - `awk '/^## Troubleshooting$/,/^## License$/' README.md | grep -E "^\\| [A-Z]" | wc -l` returns exactly 4 (the header row "| Symptom..." + 3 data rows; the separator `| ---` starts with a different letter so is excluded by `[A-Z]`).
      - Tighter: `awk '/^## Troubleshooting$/,/^## License$/' README.md | grep -cE "^\\| (Sessions|No notes|Approval)"` returns exactly 3.
    - No `Liferay, Inc.` text introduced:
      - `awk '/^## Troubleshooting$/,/^## License$/' README.md | grep -c "Liferay, Inc."` returns 0.
    - Upgrading and License sections are unchanged (regression check):
      - The line `git pull` still appears once: `grep -c "git pull" README.md` returns 1 (unchanged).
      - `## License` content first line: `awk '/^## License$/{getline; getline; print; exit}' README.md` outputs `Apache 2.0. See [LICENSE](LICENSE) for the license text and [NOTICE](NOTICE) for copyright attribution.` exactly.
    - The full test suite still passes (no test reads README.md, but run as sanity):
      - `npm test` exits 0 with ≥ 256 passing tests (unchanged from Plan 06-02 close).
  </acceptance_criteria>
  <done>
    `README.md` contains a new `## Troubleshooting` section between
    `## Upgrading` and `## License`. The section has the verbatim opener,
    exactly three table data rows, four doctor check labels quoted
    verbatim from `src/commands/doctor.js`, both Tier-2 drift tokens
    (`i hooks:`, `i permissions:`) referenced in the parallel Row 1 / Row 3
    check columns, no inlined sample output, no emojis. Upgrading and
    License sections are unchanged. The test suite still passes.
  </done>
</task>

</tasks>

<verification>
Phase-level verification (this is the final plan in Phase 06):

1. `grep -c "^## Troubleshooting$" README.md` returns 1.
2. Section ordering: Upgrading < Troubleshooting < License in README.md.
3. Every doctor check label quoted in README.md Troubleshooting appears verbatim in `src/commands/doctor.js` (cross-source identity check):
   - `hooks merged in settings.json`
   - `vault config present`
   - `vault path exists on disk`
   - `permissions merged in settings.json`
   - `i hooks:` (Tier-2 drift token, Row 1)
   - `i permissions:` (Tier-2 drift token, Row 3)
4. The opening sentence is the verbatim D-NO-SAMPLE-locked string.
5. The table has exactly 3 data rows (not 7, not 1) matching INST-03's named symptoms.
6. Row 1 and Row 3 are structurally parallel (Tier-1 ✗ OR Tier-2 drift line) — Row 1's `i hooks:` token closes the "post-upgrade hook drift" diagnostic gap.
7. Row 2's symptom text uses the REQ-INST-03 verbatim "no notes captured" phrasing.
8. No fenced code block (no inlined doctor sample output).
9. No emojis — only U+2713 ✓ and U+2717 ✗ Unicode geometric symbols.
10. `## Upgrading` and `## License` content is unchanged.
11. Phase 06 closes with all three requirements satisfied:
    - INST-01: doctor command exists with 7 checks, summary, exit code (Plan 06-01).
    - INST-02: every ✗ has a one-line `→ ` remediation hint AND those remediations point at real commands (Plans 06-01 + 06-02).
    - INST-03: README Troubleshooting section maps symptoms to doctor checks (Plan 06-03).
12. `npm test` exits 0 with ≥ 256 passing tests.
</verification>

<success_criteria>
- The single task's `<verify>` block passes.
- All acceptance criteria above are met.
- INST-03 is fully addressed.
- Cross-source label identity is maintained: every label in README.md Troubleshooting Column 2 appears verbatim in `src/commands/doctor.js`, including both Tier-2 drift tokens (`i hooks:`, `i permissions:`).
- The Troubleshooting section is the minimal three-row form per D-SYMPTOM-ROWS — no overshoot to one-row-per-check.
- Row 1 and Row 3 share the symmetric "Tier-1 ✗ OR Tier-2 drift" shape; Row 1's `i hooks:` token closes the post-upgrade-hook-drift diagnostic gap.
- Row 2 symptom is REQ-INST-03 verbatim ("no notes captured").
- No drift between doctor's actual output and the README description (because D-NO-SAMPLE keeps the README at the pointer-level, not the rendering-level).
- Emoji-absence check uses grep PCRE only — no python3 runtime dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/06-install-ux-hardening/06-03-SUMMARY.md`
following the template at $HOME/.claude/get-shit-done/templates/summary.md.
Key facts to record:
- The exact placement (between Upgrading and License — Phase 07 will append Support/Feedback after License).
- The D-NO-SAMPLE decision honored (no inlined sample, immune to doctor-output drift).
- The cross-source label identity check that proves doctor.js and README.md agree on the seven labels.
- The symmetric Row 1 / Row 3 shape (Tier-1 ✗ OR Tier-2 drift) and why it matters for post-upgrade hook drift diagnosis.
- The Row 2 verbatim REQ-INST-03 phrasing ("no notes captured").
- Phase 06 closes here — all three requirements (INST-01, INST-02, INST-03) satisfied.
</output>
